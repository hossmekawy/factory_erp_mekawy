[Unit]
Description=Run the cutting digest once a day

[Timer]
# 07:00 Africa/Cairo — before the shift, so the shortage from yesterday is on
# the supervisor's phone when he walks in.
OnCalendar=*-*-* 07:00:00
Persistent=true

[Install]
WantedBy=timers.target
