# تقرير الاستكشاف — الربط بالـ HR وتصميم موديول `cutting`

**المرحلة:** ٠ — تصميم، **مفيش كود ولا migrations ولا تعديل على ملف قايم**
**التاريخ:** 31 أغسطس 2026
**المراجع:** `SRS-cutting-bank-rolls.md` v1.8 · `CLAUDE-CODE-BRIEF.md` · `reference/notebook-page-1749.jpeg`

> **القسم (أ) — خريطة النظام الحالي** موجود بالكامل في `PHASE1-DELETION-AUDIT.md`
> (الـ apps، الإصدارات، الإعدادات، الفرونت، الصلاحيات، أنماط الـ API).
> التقرير ده بيكمّل الأقسام **ب → و**.

---

## ب. نقط الاشتباك مع الـ HR

### ب.١ — `Employee` (في `hr/models.py`)

مالوش أي علاقة بـ `User`. كيان منفصل تماماً — الموظف مش مستخدم للنظام، والعكس.

| الحقل | النوع | ملاحظة |
|---|---|---|
| `employee_code` | `CharField(max_length=20, unique=True)` | **ده مفتاح الربط بجهاز البصمة** |
| `full_name` | `CharField(200)` | |
| `department` | `FK → Department (SET_NULL, null)` | |
| `job_title` | `CharField(100, blank)` | |
| `national_id` | `CharField(14, blank)` | |
| `phone_number` | `CharField(15, blank)` | |
| `address` | `TextField(blank)` | |
| `gender` | `CharField(choices: male/female)` | |
| `birth_date` · `hire_date` | `DateField(null)` | `hire_date` مستخدم في التقرير الأسبوعي لتمييز "قبل التعيين" عن "غياب" |
| `salary` | `Decimal(10,2, null)` | |
| `photo` · `id_front_image` · `id_back_image` | `ImageField(null)` | |
| `is_active` | `Boolean(default=True)` | **كل الـ ٢٢ موظف نشطين** |
| `created_at` · `updated_at` | auto | |

`Meta`: `ordering = ["full_name"]`.

> ### ⚠️ تعارض ١ — اسم حقل البصمة
> الـ SRS بند 4.2 و بند 6 بيقولوا إن المفتاح اسمه **`device_user_id`**.
> **الحقل ده مش موجود.** الاسم الحقيقي **`employee_code`**، والـ verbose_name بتاعه
> حرفياً "كود الموظف (رقمه على جهاز البصمة)". يعني نفس المعنى، اسم مختلف.
> **مش هعمل rename** — ده هيكسر `devices/` كله. هستخدم `employee_code`.

> ### ⚠️ تعارض ٢ — `is_team_leader` مش موجود
> الـ SRS بند 4.2 بيفترض وجوده. مش موجود. اقتراح التعديل في القسم (ج).

### ب.٢ — موديلات الحضور (في `devices/models.py`)

**`AttendanceLog` — بصمات خام، مش سجل حضور/انصراف.**

| الحقل | النوع | ملاحظة |
|---|---|---|
| `device` | `FK → Device (CASCADE, null)` | |
| `employee` | `FK → Employee (SET_NULL, null)` | ⚠️ اقرا التحذير تحت |
| `employee_code` | `CharField(20)` | **دايماً مليان — ده المفتاح الموثوق** |
| `timestamp` | `DateTimeField` | UTC مخزّن، بيتحوّل لـ Africa/Cairo بـ `timezone.localtime()` |
| `punch_state` | `SmallInteger(default=0)` | نوع الحركة من الجهاز |
| `verify_type` | `SmallInteger(default=1)` | |
| `source` | `device` / `manual` | |

- `UniqueConstraint(device, employee_code, timestamp)` — بيمنع التكرار عند إعادة الإرسال.
- `Index(employee_code, timestamp)` — **الفهرس ده بالظبط اللي محتاجينه** لاستعلامات الفترات.
- `ordering = ["-timestamp"]`.

**السجل واحد لكل بصمة** — مش check-in/check-out منفصلين ولا سجل يومي. الساعات **مش مخزّنة في أي مكان**، بتتحسب وقت العرض.

> ### 🔴 تحذير مهم — متربطش بـ `employee` FK
> فحصت البيانات الفعلية: **٢٨٢ سجل من ١٦٧٣ (١٧٪) عندهم `employee_id = NULL`**،
> و**٧ أكواد في سجلات البصمة مالهمش موظف مقابل** في `hr_employee` أصلاً.
>
> يعني لو عملت `join` على الـ FK هتخسر ١٧٪ من البصمات من غير ما تحس.
> **لازم الربط يكون على `employee_code`** — وده بالظبط اللي `hr/reports.py` بيعمله.

### ب.٣ — إزاي بيتحسب الحضور دلوقتي؟

**مفيش دالة ولا manager ولا service جاهز.** فيه **تطبيق واحد وحيد**، مكتوب **جوه** دالة، ومش قابل لإعادة الاستخدام:

`hr/reports.py::build_weekly_report(anchor)` — بيبني أسبوع كامل (سبت→خميس) **لكل الموظفين النشطين مرة واحدة**، والقاعدة جواه:

```python
times = sorted(punches.get((emp.employee_code, day), []))
if times:
    first, last = times[0], times[-1]
    hours = round((last - first).total_seconds() / 3600, 2) if last > first else 0
```

**القاعدة: أول بصمة في اليوم = حضور، آخر بصمة = انصراف، الساعات = الفرق بينهم.**
`punch_state` **بيتجاهَل تماماً** رغم إنه متسجل.

المكان التاني الوحيد `EmployeeViewSet.attendance` action في `hr/views.py:117` — بيرجّع **البصمات الخام** مفلترة بالتاريخ، **من غير أي حساب ساعات**.

**النتيجة العملية:** الدالتين اللي محتاجهم من `cutting` **مش موجودين ومحتاج نكتبهم**:

```python
# مش موجود — محتاج نكتبه
attendance_days(employee_codes, start_date, end_date) -> dict[code, set[date]]
attendance_hours(employee_code, start_date, end_date) -> float
```

**الاستعلام اللي هستخدمه** (بيستفيد من `Index(employee_code, timestamp)` الموجود):

```python
tz = timezone.get_current_timezone()
range_start = datetime.combine(start_date, time.min, tzinfo=tz)
range_end   = datetime.combine(end_date + timedelta(days=1), time.min, tzinfo=tz)

logs = (AttendanceLog.objects
        .filter(employee_code__in=codes,
                timestamp__gte=range_start, timestamp__lt=range_end)
        .values_list("employee_code", "timestamp"))
# ثم نفس قاعدة أول/آخر بصمة لكل (code, local_date)
```

> ### 🔴 مشكلة حقيقية في البيانات — الإنتاجية هتطلع غلط
> فحصت البيانات: **٢٣٥ يوم-موظف من ٩٤٠ (٢٥٪) فيهم بصمة واحدة بس**.
> بقاعدة "آخر ناقص أول"، اليوم ده **ساعاته = صفر**.
>
> الـ SRS بند 5.6 بيقول **الإنتاجية = القطع الفعلية ÷ ساعات حضور رئيس الفريق**.
> لو رئيس الفريق يومه فيه بصمة واحدة، المقام **صفر** — قسمة على صفر، أو إنتاجية
> لانهائية. متوسط البصمات ٢ﺍ.٧٨ في اليوم بس.
>
> ده مش خطأ في الكود، ده **واقع بيانات**. لازم قرار — سؤال ٦ في القسم (و).

> ### ⚠️ تعارض ٣ — تاريخ البصمة قصير
> سجلات البصمة بتبدأ من **٩ يوليو ٢٠٢٦** وبتنتهي **٣١ أغسطس** — **٤٦ يوم بس**.
> يعني التحقق V7 (حضور رئيس الفريق) **مش هينفع على أي فرشة قبل يوليو**، وده
> بيأكد قرار الـ SRS بند 12 بإيقاف V7 في وضع الترحيل.

### ب.٤ — الصلاحيات

`hr/permissions.py`: `role_of(user)` بيرجّع أول group من
`["admin", "hr", "production_manager", "cutting_supervisor", "cutting"]`.
`IsAdmin` و `IsAdminOrHR` موجودين. **الأدوار التلاتة بتاعة القص اتساببت** في مرحلة الحذف.

`cutting` هتحتاج classes خاصة بيها (`IsCuttingStaff` للكتابة، قراءة لمدير الإنتاج) — **جوه `cutting/permissions.py`**، بتستورد `role_of` من `hr` من غير ما تعدّل فيه.

---

## ج. التعديلات المطلوبة على النظام الحالي — **اقتراحات، لم تُنفَّذ**

| # | التعديل | الملف / الموديل | السبب | البديل لو مرفوض | الخطورة |
|---|---|---|---|---|---|
| **ج١** | `is_team_leader = BooleanField(default=False)` + migration | `hr/models.py` → `Employee` | SRS 4.2 و 6: تحديد مين يظهر في قايمة رؤساء الفرق. من غيره القايمة هتبقى ٢٢ موظف كلهم | قسم اسمه "رؤساء فرق" في `Department`، أو جدول `TeamLeader` جوه `cutting` بـ FK على Employee | 🟢 **منخفضة** — حقل جديد بـ default، مالوش أثر على أي كود قايم |
| **ج٢** | إضافة `"is_team_leader"` لقائمة `fields` | `hr/serializers.py` → `EmployeeSerializer` | عشان الأدمن يقدر يعلّم الموظف من شاشة الموظفين | إدارته من Django admin بس | 🟢 منخفضة — حقل إضافي في response موجود |
| **ج٣** | خانة/عمود `is_team_leader` في شاشة الموظف | `frontend/app/employees/*` · `components/EmployeeForm.tsx` | نفس السبب | Django admin | 🟡 متوسطة — تعديل على شاشة شغالة |
| **ج٤** | **ملف جديد** `hr/attendance.py` فيه `attendance_days()` و `attendance_hours()` | ملف جديد، **مفيش تعديل على ملف قايم** | V7 + حساب الإنتاجية. المنطق دلوقتي محبوس جوه `build_weekly_report` ومش قابل للاستدعاء | نكتب نفس المنطق جوه `cutting/services.py` | 🟢 **منخفضة** — ملف جديد، مالوش أي أثر |
| **ج٥** | تعديل `build_weekly_report` تستدعي `hr/attendance.py` بدل الكود المكرر | `hr/reports.py` | عشان قاعدة "أول/آخر بصمة" تبقى في مكان واحد بدل اتنين | نسيب التكرار ونعلّم عليه بتعليق | 🔴 **عالية** — بيلمس التقرير الأسبوعي الشغّال |
| **ج٦** | `Employee.user = OneToOne → User (null)` | `hr/models.py` | يخلي مشرف القص يترجم لموظف | — | ⚪ **مؤجّل** — مش محتاجينه في المرحلة الأولى |

### توصيتي

**نفّذ ج١ + ج٢ + ج٤ بس دلوقتي.** التلاتة دول إضافات نضيفة (حقل بـ default، سطر في serializer، ملف جديد) ومالهمش أي أثر على شغل شغّال.

**ج٥ متعملهوش دلوقتي.** التقرير الأسبوعي شغال ومحدش اشتكى منه. هسيب تعليق في `hr/attendance.py` بيقول إن نفس القاعدة متكررة في `hr/reports.py` وإن التوحيد مؤجّل. لو حبيت نوحّدهم بعدين، هعمله في PR لوحده مع مقارنة مخرجات التقرير قبل وبعد للتأكد إنها متطابقة.

**ج٣** ممكن يستنى لحد ما نحتاج نعلّم رؤساء الفرق فعلياً — مؤقتاً من Django admin.

---

## د. الفجوات والتعارضات بين الـ SRS والواقع

### د.١ — 🔴 مثال توزيع القطع في الـ SRS **حسابه غلط**

الـ SRS بند 4.9 بيدّي مثال: طقم `30 32 33 34 36 36 38 38 38` (= ٩ قطع في الراق)، والإجمالي **٤٩٠**:

> «38 → 163 · 36 → 109 · 30/32/33/34 → 54 لكل واحد»

**المجموع = 163 + 109 + (54×4) = 488، مش 490.** ناقص قطعتين.

والأهم: **القاعدة المكتوبة والمثال المكتوب بيتناقضوا**. القاعدة بتقول «باقي القسمة بيتوزّع على أكتر المقاسات عددًا» — لو طبقناها حرفياً الباقي (٣) كله يروح لمقاس ٣٨ فيبقى ١٦٦، لكن المثال بيقول ٣٨ → ١٦٣.

الحساب الصحيح:

| المقاس | النسبة | ٤٩٠ × النسبة | الجزء الصحيح | الكسر |
|---|---|---|---|---|
| 38 | 3/9 | 163.33 | 163 | .33 |
| 36 | 2/9 | 108.89 | 108 | **.89** |
| 30 · 32 · 33 · 34 | 1/9 لكل | 54.44 | 54 لكل | .44 لكل |
| | | | **مجموع = 487** | **الباقي = 3** |

فيه حلين، والاتنين بيوصلوا لـ ٤٩٠ بالظبط:

**(أ) أكبر باقي (Largest Remainder / Hamilton)** — الباقي يروح لأصحاب أكبر كسر:
`36 → 109` · اتنين من مقاسات الـ 1/9 → `55` · والاتنين التانيين → `54` · `38 → 163`
✅ بيطابق رقمين من التلاتة المكتوبين في الـ SRS (163 و 109).

**(ب) حرفياً حسب القاعدة المكتوبة** — الباقي كله لأكبر مقاس عدداً:
`38 → 166` · `36 → 108` · الباقي `54`
❌ بيخالف الرقم ١٦٣ المكتوب في المثال.

**اقتراحي: (أ)**، لأنه بيطابق أغلب أرقام المثال، وهو الأسلوب القياسي في توزيع الحصص، وبيوزّع الفروق بعدل بدل ما يكوّمها على مقاس واحد. التعادل يُفَض بـ `pieces_in_ply` الأكبر ثم ترتيب المقاس. **سؤال ١.**

### د.٢ — 🔴 `roll_id` كـ FK على جدول ممنوع وجوده

الـ SRS بند 4.8 بيقول `roll_id | FK → FabricRoll (null)`، وبند 4.3 بيقول صراحة **«مفيش `FabricRoll` كجدول مخزون»**. **مينفعش FK على موديل مش موجود** — Django مش هيقبلها.

**اقتراحي:** `roll_ref = PositiveIntegerField(null=True, blank=True, db_index=True)` — عدد صحيح عادي فاضي دلوقتي، ويتحوّل لـ FK حقيقي بـ migration في المرحلة ٣. ده بيحقق نية الـ SRS (العمود موجود من دلوقتي) من غير التناقض. **سؤال ٢.**

### د.٣ — ⚠️ اسم الكلاس `Model` بيتعارض مع Django

الـ SRS بند 4.4 بيسمّي كتالوج الموديلات `Model`. `class Model(models.Model)` اسم كارثي — بيحجب `django.db.models.Model` ويلخبط أي قارئ.

**اقتراحي:** الكلاس اسمه `GarmentModel`، والجدول `cutting_garmentmodel`، والـ endpoint يفضل `/api/cutting/models/` زي ما الـ SRS كاتب. **سؤال ٣.**

### د.٤ — ⚠️ حالة `وصل` (splice) غير محدّدة بما يكفي للتنفيذ

الـ SRS بند 4.8 بيقول: «التوب خلص واتوصل بتوب جديد لإكمال نفس الراق — **الراق يتحسب مرة واحدة على السطرين**».

مش واضح **إزاي**. لو السطر الأول `plies=15` والتاني `plies=10`، الإجمالي يبقى ٢٥؟ ٢٤ (طبقة مشتركة اتحسبت مرتين)؟ ١٥؟

فيزيائياً: التوب خلص في نص طبقة، فاتوصل توب تاني عشان يكمّل **نفس الطبقة**. يعني فيه **طبقة واحدة** مشتركة بين السطرين.

**اقتراحي:** `إجمالي الراق = Σ plies − (عدد السطور المعلّمة "وصل")` — كل وصلة بتطرح طبقة واحدة مشتركة. و`has_splice` على الفرشة يبقى `True` لو فيه أي سطر كده. **سؤال ٤.**

### د.٥ — ⚠️ الدفتر نفسه فيه أقل من اللي الـ SRS طالبه

بصّيت على `reference/notebook-page-1749.jpeg` (الصورة مقلوبة ٩٠° في الملف الأصلي — محتاجة تدوير قبل العرض).

**اللي مكتوب في ترويسة الدفتر، بالترتيب من فوق لتحت:**
`التاريخ ٩/٧/٢٠٢٦` · `الكود ١٧٤٩` · `المقاسات` · `طول الفرشة` · `عرض الفرشة` · `عدد القطع`

**جسم الصفحة:** عمود سطور، كل سطر `توب ن` وجنبه رقم الراق وطول التوب بالمتر (`١٦`، `١٦`، `١٥`، `١٥`، `١٥`، `١٥`، `١٣` …) — وده **بيأكد تصميم `LayLine`**: سطر لكل توب فيه `plies` و `roll_length_m`.

**بس:**
1. **مفيش بنك مكتوب في الدفتر، ومفيش رئيس فريق.** الـ SRS بند 7.2 بيقول الشاشة تقلّد الدفتر بالظبط، وفي نفس الوقت بيطلب الحقلين دول في القسم العلوي. الحقلين دول **إضافة على الدفتر مش نقل منه** — لازم يتحطوا في مكان مايعطّلش النقل السريع.
2. **المقاسات مكتوبة بين أقواس** `(٣٢)(٣٤)(٣٦)…` مش مفصولة بمسافات زي مثال الـ SRS `30 32 32 34 34 36`. **الـ parser لازم يقبل الشكلين** (وكمان الفاصلة والشرطة) والأرقام العربية والإنجليزية.
3. **الكود ١٧٤٩ مكتوب في الدفتر** كترويسة الصفحة. في الـ SRS الكود بتاع `GarmentModel` مش بتاع `Lay`. الصف القديم في الداتابيز كان `code=1749` و `production_order_no=1749` — نفس الرقم في الاتنين. **سؤال ٥.**

> **ملحوظة على الأمانة:** الخط اليدوي في الصورة مش كله واضح عندي. القراءات فوق (ترتيب الترويسة، وجود سطور الأتواب، الأقواس حوالين المقاسات) واثق منها؛ **القيم الرقمية بالتحديد لأ**. إنت هتراجعها بنفسك في المرحلة ج زي ما اتفقنا.

### د.٦ — ⚠️ حاجات في البنية التحتية مش موجودة

| الـ SRS طالب | الواقع | الحل المقترح |
|---|---|---|
| Celery أو django-q للإيميل المجمّع (بند 11.1) | **مفيش أي task queue** | management command + `systemd timer` يومي. أبسط بكتير من إضافة broker لمصنع واحد |
| خط **Tajawal** (بند 10) | المشروع كله على **Cairo** | سؤال ٧ |
| Audit log (بند 10) | **مفيش** `django-simple-history` ولا غيره | جدول `LayAudit` صغير جوه `cutting` — ٦ أعمدة، أخف من مكتبة كاملة |
| إعدادات التسامح والبواقي قابلة للتعديل (بند 5.3، 5.4) | مفيش مكان لإعدادات القص | `CuttingSettings` singleton بنفس نمط `WorkSchedule.get_solo()` الموجود |
| Offline / IndexedDB (بند 7.2) | مفيش | خارج المرحلة أ→ب. الـ API هيتصمم بـ idempotency key من دلوقتي عشان مايعطّلهاش |

### د.٧ — ⚠️ البحث بالصيغ المختصرة العربية

الـ SRS بند 7.1.1 عايز `ميتراج>1.2` · `عجز:نعم` · `مقاس:32` · `حالة:مستنية-ترقيم`.
ده **parser كامل بلغة استعلام عربية** — شغل مش بسيط، وقيمته أقل بكتير من الفلاتر المنظّمة في 7.1.2 اللي بتعمل نفس الحاجة بواجهة أوضح.
**اقتراحي:** المرحلة (د) تعمل البحث الحر + الفلاتر المنظّمة، والـ token search يتأجل لحد ما تستخدم النظام وتقولي إنك محتاجه فعلاً. **سؤال ٨.**

---

## هـ. التصميم المقترح

### هـ.١ — بنية الـ app

```
backend/cutting/
├── models.py        الكيانات التسعة
├── services.py      كل الحسابات — المصدر الوحيد للحقيقة
├── sizes.py         تفكيك نص المقاسات + توزيع القطع الفعلية
├── validators.py    V1 → V9
├── serializers.py   DRF
├── views.py         ViewSets رفيعة — بتنادي services، مفيهاش حساب
├── filters.py       django-filter
├── permissions.py   بتستورد role_of من hr
├── admin.py
├── reports.py       نفس نمط hr/reports.py (dict واحد → JSON/Excel/PDF)
├── notifications.py المرحلة (و)
├── management/commands/send_cutting_digest.py
├── migrations/
└── tests/
    ├── test_calculations.py   المعادلات
    ├── test_sizes.py          التفكيك والتوزيع
    ├── test_validators.py     V1→V9
    └── test_lay_dates.py      الفرشة الممتدة والفلترة بالتقاطع
```

### هـ.٢ — الموديلات

**`Bank`** — `code` (unique) · `name` · `is_active`

**`GarmentModel`** — `code` (unique, indexed) · `name` · `category` · `fit` · `default_size_set` (FK→SizeSet, null, SET_NULL) · `image` · `notes` · `is_active`

**`SizeSet`** — `name` · `sizes_raw` (CharField 200) · `total_pieces` (Integer, **محسوب ومخزّن** في `save()`)

**`Lay`** — الكيان الأساسي:

| مجموعة | الحقول |
|---|---|
| التواريخ | `start_date` · `end_date` (**غير فارغ**، بيتملّي بـ `start_date` لو مادخلش — اقرا تحت) |
| العلاقات | `bank` (PROTECT) · `garment_model` (PROTECT) · `size_set` (SET_NULL, null) · `team_leader` (FK→Employee, PROTECT) · `team_members` (M2M→Employee, blank) · `entered_by` (FK→User, PROTECT) |
| القياسات | `lay_width_cm` Dec(6,2) · `narrowest_width_cm` Dec(6,2) null · `lay_length_m` Dec(8,2) · `pieces_per_ply` PositiveInt |
| الحالة | `status` (open/closed/counted/approved) · `entry_mode` (detailed/quick) · `is_backfill` Bool · `has_splice` Bool |
| المستندات | `sheet_image` (ImageField → `cutting/%Y/%m/`) · `notes` |
| **محسوبة ومخزّنة** | `total_plies` · `theoretical_pieces` · `total_roll_length_m` · `total_remnant_m` · `consumed_m` · `fabric_shortage_m` · `expected_metrage` · `real_metrage` (null) · `deviation_pct` (null) · `has_shortage` Bool |
| Audit | `created_at` · `updated_at` · `closed_at` · `closed_by` · `client_uuid` (unique, null — للـ idempotency) |

**`LaySizeBreakdown`** — `lay` (CASCADE) · `size` (Char 20) · `pieces_in_ply` · `actual_pieces` (null) · `is_manually_adjusted` (Bool) · `order` (Int)
`UniqueConstraint(lay, size)`

**`LayLine`** — `lay` (CASCADE) · `line_no` · `roll_ref` (PositiveInt null — بدل الـ FK، اقرا د.٢) · `article` · `lot_no` · `roll_no` · `shade_note` · `ticket_image` · `ticket_data` (JSON) · `roll_length_m` · `plies` · `remnant_m` · `width_cm` (null) · `net_weight_kg` (null) · `barcode` (Char blank) · `roll_end_action` · `remnant_disposition` · `is_aggregate` (Bool) · `notes`
`UniqueConstraint(lay, line_no)`

**`LayOutput`** — `lay` (OneToOne, CASCADE) · `actual_pieces` · `rejected_pieces` · `recorded_by` (FK→User) · `recorded_at` · `notes`

**`RemnantLog`** — `lay_line` (OneToOne, CASCADE) · `length_m` · `shade_note` · `lot_no` · `article` · `disposition` (waste/usable) · `logged_at`

**`CuttingSettings`** — singleton بنمط `get_solo()`: `fabric_tolerance_pct` (0.5) · `pieces_tolerance_pct` (2.0) · `remnant_waste_threshold_m` (1.00) · `notify_emails`

**`LayAudit`** — `lay` (CASCADE) · `user` · `at` · `action` · `field` · `old_value` · `new_value` · `reason`

### هـ.٣ — الـ indexes

| الجدول | الفهرس | السبب |
|---|---|---|
| `Lay` | `(start_date, end_date)` | **الفلترة بالتقاطع** — أهم استعلام في النظام |
| `Lay` | `(status, end_date)` | "مقفولة ومستنية ترقيم" + التنبيه بعد ٢٤ ساعة |
| `Lay` | `team_leader` · `bank` · `garment_model` | فلاتر بند 7.1.2 |
| `Lay` | `has_shortage` · `is_backfill` | فلاتر + استبعاد المرحّلة من التقارير |
| `Lay` | `deviation_pct` · `real_metrage` | الفرز والفلترة بمدى (بند 7.1.2) |
| `LayLine` | `(lay, line_no)` unique | |
| `LayLine` | `article` · `lot_no` | تقرير العجز حسب اللوط (بند ٩.٢) |
| `LaySizeBreakdown` | `(lay, size)` unique · `size` | فلتر "مقاس معيّن موجود في الفرشة" |

### هـ.٤ — الحسابات: فين وليه

**القرار: `services.py` هي المصدر الوحيد، والنتايج بتتخزن على `Lay` كأعمدة.**

النية دي بتتعارض مع الحدس (property أنضف)، فدي الأسباب:

**١. الـ SRS بيفرض التخزين.** بند 7.1.2 بيطلب فلترة **بمدى** على `الانحراف %` و`الميتراج الحقيقي`، وفرز بأي عمود، وفلتر `فيها عجز؟`. **`django-filter` مايقدرش يفلتر على Python property** — لازم عمود في الداتابيز. ده السبب الحاسم، مش تحسين أداء.

**٢. الـ property بتعمل N+1.** قايمة ٥٠ فرشة × حساب بيمشي على السطور = ٥٠ استعلام إضافي على الأقل. الـ `annotate` مايقدرش يعبّر عن قاعدة الوصل ولا التوزيع.

**٣. الحسابات مش نقية زمنياً.** `real_metrage` مالهاش قيمة قبل الترقيم، و`fabric_shortage_m` بتتثبّت عند القفل. الفرشة المعتمدة أرقامها **snapshot** — لو غيّرنا معادلة بعدين، الفرشات القديمة مايتغيروش بأثر رجعي. الـ property هتغيّرهم كلهم.

**إزاي بتشتغل:**

```
الفرونت (لايف، < 200ms)  →  نفس المعادلات بالـ TypeScript، للعرض بس
        ↓ عند القفل / الترقيم
services.recalculate(lay)  →  بيرجّع dict
        ↓
Lay.objects.filter(pk=...).update(**dict)   ← الباك هو المرجع
```

الفرونت بيحسب ليعرض، الباك بيحسب ليحفظ، **والباك هو الحكم** (قرار البريف بند ٣). المعادلات مكتوبة مرتين بقصد — والتستات بتقفل على أرقام الباك.

**اللي هيفضل property (رخيص ومش مفلتر عليه):**
`working_days` = `(end_date − start_date).days + 1` · `expected_metrage` وقت العرض للفرشة المفتوحة · تصنيف الباقي هالك/صالح (مقارنة واحدة).

**`end_date` غير فارغ — ليه أخالف الـ SRS هنا:**
الـ SRS بيقول `Date (null)` و«لو فاضي يبقى نفس يوم البداية». المعنى واحد، لكن لو خزّنته `NULL` كل استعلام تقاطع هيحتاج `COALESCE(end_date, start_date)` — وده **بيلغي استخدام الفهرس**. لو ملّيته بـ `start_date` عند الإنشاء، فلتر التقاطع بيبقى:

```sql
WHERE start_date <= :to AND end_date >= :from
```

استعلام بسيط بيستخدم الفهرس. نفس السلوك بالظبط، أداء أحسن. **سؤال ٩.**

### هـ.٥ — ترتيب التنفيذ

| المرحلة | المحتوى | الحجم النسبي | التسليم القابل للاختبار |
|---|---|---|---|
| **أ** | الموديلات + migrations + `services.py` + `sizes.py` + `validators.py` + التستات | **كبيرة** | `pytest` أخضر على كل المعادلات و V1→V9، من غير أي API |
| **ب** | Serializers + ViewSets + `close` و `output` + `filters.py` + الصلاحيات | متوسطة | كل الـ endpoints شغالة بـ curl، فلاتر بند 7.1.2 بتشتغل |
| **ج** | شاشة "فرشة جديدة" موبايل + الوضعين + شريط الحسابات اللايف + الكاميرا | **كبيرة** | إنت بتدخّل صفحة دفتر حقيقية من موبايلك |
| **د** | القايمة + التفاصيل + البحث + الفلاتر في الـ URL | كبيرة | |
| **هـ** | شاشة الترقيم + التوزيع الأوتوماتيكي + تأكيد التعديل اليدوي | صغيرة | |
| **و** | `Notification` + الإيميل المجمّع + تقارير الميتراج والعجز | متوسطة | |

**نطاق المرحلة أ بالظبط:** الموديلات التسعة، `makemigrations` + `migrate`، `services.py`، `sizes.py`، `validators.py`، و tests على: الميتراج المتوقع، الميتراج الحقيقي (على القطع الفعلية)، توزيع القطع، العجز، تفكيك نص المقاسات، الفرشة الممتدة والفلترة بالتقاطع. **مفيش serializers ولا views ولا فرونت.**

**ملحوظة على التستات:** المشروع **مالوش ولا تست واحد** حالياً. المرحلة أ هتضيف `pytest` + `pytest-django` لـ `requirements.txt` وملف `pytest.ini`. لو تفضّل `manage.py test` المدمج من غير أي dependency جديدة، قولي. **سؤال ١٠.**

---

## و. أسئلة محتاج إجابتها قبل ما أبدأ المرحلة أ

| # | السؤال | اقتراحي |
|---|---|---|
| **١** | توزيع باقي القسمة: **أكبر باقي** ولا **كله لأكبر مقاس**؟ (مثال الـ SRS متناقض مع قاعدته وحسابه غلط بقطعتين — د.١) | **أكبر باقي (Hamilton)** — بيطابق أغلب أرقام المثال وبيوزّع بعدل |
| **٢** | `roll_id` كـ `PositiveIntegerField` بدل FK على جدول ممنوع وجوده؟ (د.٢) | **نعم** — `roll_ref` integer فاضي، يتحوّل FK في المرحلة ٣ |
| **٣** | اسم الكلاس `GarmentModel` بدل `Model`؟ (د.٣) | **نعم**، والـ endpoint يفضل `/api/cutting/models/` |
| **٤** | حساب الراق مع الوصل: `Σ plies − عدد الوصلات`؟ (د.٤) | **نعم** — كل وصلة بتطرح طبقة مشتركة واحدة |
| **٥** | الكود ١٧٤٩ اللي في الدفتر: ده كود الموديل، ولا الفرشة محتاجة رقم خاص بيها كمان؟ | **كود الموديل**. لو الفرشات بتتراجع برقم، أضيف `Lay.ref_no` اختياري |
| **٦** | 🔴 يوم رئيس الفريق فيه بصمة واحدة = **صفر ساعات** (٢٥٪ من الأيام). الإنتاجية تتحسب إزاي؟ | **نستبعد الأيام دي من المقام** ونعرض "مبني على ن يوم من أصل م". لو مفيش ولا يوم صالح نعرض "غير متاح" بدل رقم غلط |
| **٧** | خط **Tajawal** زي الـ SRS ولا **Cairo** زي باقي النظام؟ | **Cairo** — اتساق النظام أهم، والفرق بينهم بسيط. لو مصرّ على Tajawal أغيّره للنظام كله مش لصفحات القص بس |
| **٨** | البحث بالصيغ المختصرة العربية (`ميتراج>1.2`) في المرحلة (د) ولا يتأجل؟ | **يتأجل** — الفلاتر المنظّمة بتعمل نفس الشغل بواجهة أوضح |
| **٩** | `end_date` غير فارغ (بيتملّي بـ `start_date`) بدل null؟ (هـ.٤) | **نعم** — نفس السلوك، وفلتر التقاطع بيستخدم الفهرس |
| **١٠** | `pytest` + `pytest-django` ولا `manage.py test` المدمج؟ | **pytest** — أوضح في كتابة حالات المعادلات، والمشروع مالوش تستات أصلاً فمفيش تعارض |
| **١١** | التعديلات على الـ HR: أنفّذ **ج١ + ج٢ + ج٤** بس؟ (القسم ج) | **نعم** — التلاتة إضافات نضيفة. **ج٥ متعملهوش** عشان مانلمسش التقرير الأسبوعي الشغّال |

---

**واقف هنا. مفيش ولا سطر كود اتكتب.**
المرحلة أ مش هتبدأ غير بعد إجابات الأسئلة ١ → ١١، وبالذات **١ و ٤ و ٦** — دي بتغيّر المعادلات نفسها.
