# SRS — موديول القص والبنك والأتواب (Cutting / Spreading Module)

**الإصدار:** 2.6 — الراق لكل لون
**التاريخ:** 1 سبتمبر 2026
**النطاق:** تسجيل عمليات الفرد والقص على البنوك، وربطها بالأتواب وبرئيس الفريق المسجَّل على جهاز البصمة، مع شاشة بسيطة لتسجيل القطع الفعلية بعد الترقيم.

---

## 1. الهدف والواقع الحالي

### الوضع دلوقتي
- الصنايعية بيكتبوا في **دفتر ورق** على البنك.
- **المشرف** بيدخّل البيانات بعدين — **الأساسي من الموبايل**، وأحيانًا من الكمبيوتر.
- ساعات بيبقوا مستعجلين فبيكتبوا **إجمالي الأمتار على طول** من غير تفصيل توب بتوب.
- **مفيش مخزون أتواب، ولا حتى عدد الأمتار الموجودة في المخزن** — فالمرحلة الأولى بتشتغل **من غير أي رصيد مخزني**، والمخزون المتطور بييجي في مرحلة لاحقة بعد جرد فعلي.
- **مفيش ترقيم/بندلة على السيستم** — القطع الفعلية بتتعرف هناك بس مش متسجلة.

### الهدف
1. المشرف يدخّل الفرشة من الموبايل في أقل وقت ممكن، بنفس ترتيب الدفتر.
2. الحسابات (الميتراج المتوقع والحقيقي، إجمالي القطع، البواقي، العجز) تطلع أوتوماتيك.
3. كل فرشة مربوطة **برئيس الفريق** المتعرَّف عليه من الـ HR/البصمة.
4. تسجيل القطع الفعلية بعد الترقيم عشان الميتراج الحقيقي يبقى صح.
5. صورة ورقة الدفتر وصورة تيكت التوب كمرفقات للمراجعة.

### خارج النطاق
المارکر، البندلة الكاملة، مراحل ما بعد القص (خياطة/غسيل/تشطيب)، حساب أجر الإنتاج نفسه.

---

## 2. المصطلحات

| المصطلح | المعنى | المقابل |
|---|---|---|
| البنك | ترابيزة الفرد | Spreading Bank |
| التوب | رول القماش الواحد | Fabric Roll |
| الفرشة | طبقات القماش المفرودة فوق بعض | Lay / Spread |
| الراق | عدد الطبقات | Plies |
| عرض الفرشة | عرض الفرد، محدَّد بأقل توب عرضًا | Lay Width |
| طول الفرشة | طول الفرد | Lay Length |
| عدد القطع | القطع اللي بتطلع من الراق الواحد | Pieces per Ply |
| البواقي | الفاضل من التوب اللي مكفاش راق كامل | Remnants |
| الميتراج | استهلاك القماش للقطعة | Consumption / piece |
| الخامة | اسم القماش على التيكت (BLACK MIMAS، MEGAN) | Article |
| توصيف اللون | كلام الصنايعي للتفرقة بين درجات الأتواب | Shade note |

---

## 3. الأدوار

| الدور | الصلاحيات |
|---|---|
| **مشرف القص** (`cutting_supervisor` و `cutting`) | المستخدم الأساسي. **المجموعتين ليهم نفس الصلاحيات بالظبط**؛ حساب المشرف الحقيقي هيبقى في `cutting`. يدخّل الفرشات من الموبايل، يقفل، يسجّل القطع الفعلية، و**يعدّل بعد القفل بسبب إلزامي** يتسجل في سجل النشاط. |
| **رئيس الفريق** | مش مستخدم للسيستم في المرحلة دي — بيتسجّل **كمُنفِّذ** على الفرشة بس. |
| **مدير الإنتاج / الإدارة** | قراءة + كل التقارير والداشبورد. **ميعدّلش** — بيشوف سجل التعديلات وأسبابها. |
| **الأدمن** | كل حاجة + إدارة البنوك والأكواد والمقاسات، **وهو الوحيد اللي بيمسح** من الكتالوج. |

> **صلاحية الكتالوج بالتفصيل:** مشرف القص يقدر **يضيف ويعدّل** في الموديلات والقَصّات، **بس مايمسحش**.
> السبب: هو اللي بيضيف الموديل من شاشة الفرشة (بند 7.2)، وطالما بيقدر يضيفه غلط لازم يقدر يصلّحه —
> ومنعه من التصحيح معناه إن الاسم الغلط يفضل في كل التقارير. الحذف بس هو اللي مالوش رجعة، فبيفضل للأدمن.
> وأي صف مستخدم في فرشة أو موديل، **الداتابيز نفسها بترفض حذفه** وبترجّع رسالة عربية واضحة.

> **قرار:** الصنايعية مش هيتعاملوا مع السيستم دلوقتي، فمفيش منع على أساس البصمة. البصمة بتُستخدم لـ **فلترة قايمة رؤساء الفرق** لتاريخ الفرشة (بند 6).

---

## 4. الكيانات

### 4.1 `Bank` — البنك
`code` · `name` · `is_active`

### 4.2 `Employee` (موجود في الـ HR)
- **`employee_code`** = رقم الموظف على جهاز البصمة (المفتاح الرابط). ده اسم الحقل الفعلي في الـ HR — أي ذكر سابق لـ `device_user_id` ملغي.
- `is_team_leader` = Boolean — بيحدد مين يظهر في قايمة رؤساء الفرق.

### 4.3 بيانات التوب — **بدون مخزون في المرحلة الأولى**

> **قرار أساسي:** لسه مفيش عدد أمتار الأقمشة الموجودة فعلًا في المخزن، فمينفعش نبني رصيد مخزني دلوقتي — أي رصيد هيبدأ غلط وهيفضل غلط. المرحلة الأولى **بتسجّل الاستهلاك بس، من غير أي رصيد**.

يعني في المرحلة دي:
- **مفيش** `FabricRoll` كجدول مخزون، ومفيش `remaining_m`، ومفيش خصم من رصيد.
- بيانات التوب بتتكتب **جوه سطر الفرشة نفسه** كنص، زي ما هي في الدفتر وعلى التيكت.
- العجز بيتحسب **جوه الفرشة** بس (أطوال الأتواب مقابل المستهلك + البواقي) — وده شغال تمام من غير مخزون، لأنه حساب مقفول على الفرشة.

**الحقول اللي بتتحفظ على مستوى السطر (اختيارية كلها ما عدا الطول):**
`roll_length_m` · `plies` · `remnant_m` · `shade_note` (توصيف اللون) · `lot_no` · `roll_no` · `article` (**اسم الخامة**)

> **مفيش `ticket_image`.** صورة تيكت لكل توب اتشالت — محدش في المصنع هيصوّر تيكت لكل توب، وخانة مابتتملاش أسوأ من عدم وجودها: بتفضل قدام المشرف كأنه نسي حاجة. **صورة ورقة الدفتر على مستوى القصة باقية** — دي اللي بتتصوّر فعلاً، مرة لكل قصة.

> **تنبيه على المسمّيات:** `BLACK MIMAS` و `MEGAN` دول **أسماء خامات (Article)** مش أسماء موردين، و `KB DENIM` جهة منتجة على التيكت.
>
> **المورد مش بيتسجل خالص** — لا في المرحلة دي ولا كحقل اختياري. مفيش قيمة منه دلوقتي وإدخاله هيبقى عبء على المشرف. لو احتجناه مع المخزون بعدين، بييجي من فاتورة الشراء مش من تيكت التوب.

**اسم الخامة (`article`):** نص حر بـ autocomplete من الأسماء اللي اتكتبت قبل كده، عشان الكتابة تبقى متسقة من غير ما نبني كتالوج بدري. **كتالوج الخامات الكامل بييجي مع نظام المخزون** (المرحلة 3)، وساعتها الأسماء المكتوبة دلوقتي بتتربط بيه.

### 4.3.1 الاستعداد للمخزون (مهم جدًا)
عشان الشغل اللي بيتسجّل دلوقتي ميضيعش لما نبني نظام المخزون المتطور بعدين:

1. **بنسجّل مفاتيح التعريف من دلوقتي كنص:** `article` + `lot_no` + `roll_no`. التلاتة دول بيعرّفوا التوب تعريف كافي، ودول اللي هيربطوا البيانات القديمة بجدول الأتواب لما يتبني.
2. `LayLine` هيتضاف له `roll_ref` (رقم صحيح فاضي، **مش FK**) من دلوقتي. لما المخزون يتبني، بيتحوّل لـ FK وبيتملّي بـ migration مطابقة على `article + lot_no + roll_no`.
3. **البواقي بتتسجل كسجل معلوماتي** (`RemnantLog`): الطول وتوصيف اللون واللوط والتصنيف (هالك / صالح). من غير رصيد، بس عشان نعرف كام متر بيروح هالك في الشهر. لما ييجي المخزون، السجل ده بيتحوّل لأرصدة فعلية.
4. ~~صورة التيكت بتترفع من دلوقتي~~ — **ملغي.** محدش هيصوّر تيكت لكل توب.
5. ~~`ticket_data` كحقل JSON مفتوح~~ — **ملغي مع الصورة**، كان تابع ليها.

> **القاعدة:** أي حاجة محتاجة رصيد افتتاحي → مؤجلة. أي حاجة محسوبة جوه الفرشة → شغالة من المرحلة الأولى.

### 4.4 `GarmentModel` — كتالوج الموديلات

> الاسم `Model` ممنوع لأنه بيحجب `django.db.models.Model`. الكلاس اسمه `GarmentModel`، والـ endpoint يفضل `/api/cutting/models/`.
الموديل بيتكرر كتير، فبنسجله مرة واحدة ونستدعيه.

| الحقل | النوع | مثال |
|---|---|---|
| `code` | Char (unique) | 1747 |
| `name` | Char | karl |
| `category` | Char | رجالي / حريمي / أطفال |
| `fit` | FK → Fit (null) | القَصّة (سليم / واسع / بوت كت …) — **كتالوج مش نص حر**، بند 4.4.1 |
| `default_size_set` | FK → SizeSet (null) | طقم المقاسات المعتاد |
| `notes` / `image` | | صورة الموديل (اختياري) |

> **الكود مش على الموديل — هو على القصة.** الرقم اللي فوق صفحة الدفتر (`1749`) بيخص **عملية القص دي هي**؛
> الموديل الواحد بيتقص عشرات المرات وكل مرة بتاخد كود جديد عشان القصتين مايتلغبطوش. فـ `Lay.code` هو المكتوب
> بالإيد وهو الفريد، و`GarmentModel.code` بقى **متولّد** (`1, 2, 3…`) ومجرد مقبض داخلي.
> **الموديل بيتعرّف بالاسم** — «كارل رجالي» — وده اللي بيتبحث بيه وبيتحفظ بيه.

### 4.4.1 `Category` — كتالوج الأقسام

`name` (unique) · `notes` · `order` · `is_active`

الأقسام المزروعة: **رجالي · رجالي خاص · رجالي جامبو · حريمي · بناتي · أولادي · أطفال · مواليد** — والمصنع بيزوّد عليها.

- **كل موديل لازم يحمل قسم.** ده المحور اللي التقارير بتتقرا عليه، والموديل من غير قسم بيقع بره كل فلتر.
- `GarmentModel.category` بقى `FK → Category` بـ `PROTECT` — قسم مستخدم مينفعش يتمسح.
- تعديل اسم القسم مرة واحدة بيصلّحه على **كل الموديلات** المرتبطة بيه.

**الشاشة:** `/cutting/categories` — قايمة وإضافة وتعديل وحذف، وبتعرض عدد الموديلات لكل قسم.

> **`Fit` (سليم / واسع) اتشال.** كان في الـ SRS بس مش مطلوب.

### 4.4.2 شاشة الموديلات

`/cutting/models` — CRUD كامل على الكتالوج، بتعرض لكل موديل: الكود · الاسم · القَصّة · الفئة · **عدد الفرشات المرتبطة**.
عدد الفرشات هو اللي بيحدد إذا كان الموديل ينفع يتمسح ولا لأ.

### 4.5 `SizeSet` — طقم المقاسات (قابل لإعادة الاستخدام)
| الحقل | النوع | مثال |
|---|---|---|
| `name` | Char | "رجالي عادي" |
| `sizes_raw` | Char | `30 32 34 36 38` |
| `total_pieces` | Integer (محسوب) | 5 |
| `category` | FK → Category (null) | القسم — الأطقم بتاعته بتظهر الأول |
| `is_preset` | Bool | **طقم محفوظ بقصد**، مش لقطة من قصة |

**السلوك:** المستخدم بيكتب المقاسات كنص متتابع زي ما بيقولها (`30 32 32 34 34 36`)، والسيستم:
1. بيفكّها لسطور: 30×1، 32×2، 34×2، 36×1
2. بيحسب `total_pieces` = 6
3. بيملّي `Lay.pieces_per_ply` أوتوماتيك من المجموع (مع إمكانية التعديل اليدوي)

يعني **التكرار مسموح ومقصود** — المقاس ممكن يتكرر أكتر من مرة في نفس الراق.

### 4.5.1 الأطقم المحفوظة (Presets)

نفس الأطقم بتتكرر كل يوم، وكتابتها مقاس مقاس كل مرة أبطأ حاجة في الشاشة.

- **زراير جاهزة تحت خانة المقاسات** في شاشة القصة. الضغط على الزرار **بيستبدل** المقاسات كلها بالطقم، وبعدها تعدّل عادي (تضيف مقاس، تشيل شيب).
- **الترتيب:** الأطقم بتاعة **قسم الموديل المختار** بتظهر الأول، بعدها العامة (من غير قسم)، بعدها الباقي.
- **الشاشة:** `/cutting/size-sets` — إضافة وتعديل ومسح.
- **`is_preset` بيفرّق بين الاتنين:** لما تكتب مقاسات بإيدك في قصة، بيتعمل صف `SizeSet` كمان — ده اللي التوزيع بياخد منه لقطته — بس **`is_preset=False` فمابيظهرش كزرار ولا في صفحة الأطقم**. من غير الفرق ده كل قصة بمقاسات جديدة كانت هتزوّد زرار.

### 4.6 `Lay` — الفرشة
| الحقل | النوع | مثال من الدفتر |
|---|---|---|
| `code` | Char (unique) | **كود القصة** — الرقم اللي فوق صفحة الدفتر |
| `start_date` | Date | 19/5/2026 — يوم بداية الفرد |
| `end_date` | Date (null) | يوم الانتهاء — بيتملّي عند القفل. لو فاضي يبقى نفس يوم البداية |
| `working_days` | Integer (محسوب) | عدد أيام الفرشة — بيتحسب من التاريخين |
| `bank` | FK → Bank | |
| `team_leader` | FK → Employee | **رئيس الفريق — هو المسجَّل عليه الإنتاج** |
| `team_members` | M2M → Employee (اختياري) | باقي الفريق، للعلم بس |
| `entered_by` | FK → User | المشرف اللي دخّل |
| `garment_model` | FK → GarmentModel | karl رجالي — **الكود (1747) بتاع الموديل مش بتاع الفرشة** |
| `size_set` | FK → SizeSet (null) | طقم المقاسات المستخدم |

**قيد ثابت:** الفرشة الواحدة = **موديل واحد وطقم مقاسات واحد**. مفيش خلط موديلات في نفس الفرشة.
| `lay_width_cm` | Dec(6,2) | 167 |
| `lay_length_m` | Dec(8,2) | 4.95 |
| `pieces_per_ply` | Integer | 9 |
| `entry_mode` | Choice | `تفصيلي` / `سريع` (بند 5.1) |
| `sheet_image` | Image | **صورة ورقة الدفتر — إلزامية عند القفل** (المرجع الأصلي) |
| `status` | Choice | `مفتوحة` / `مقفولة` / `مترقّمة` / `معتمدة` |
| `notes` | Text | |

### 4.7 `LaySizeBreakdown` — مقاسات الفرشة
بتتولّد من `SizeSet` المختار، وقابلة للتعديل على مستوى الفرشة الواحدة (لأن النسبة بتختلف من فرشة للتانية).

| الحقل | النوع | مثال |
|---|---|---|
| `lay` | FK → Lay | |
| `size` | Char | 32 |
| `pieces_in_ply` | Integer | 2 |
| `actual_pieces` | Integer (null) | القطع الفعلية للمقاس ده بعد الترقيم |

**قواعد:**
- `Σ pieces_in_ply` لازم يساوي `Lay.pieces_per_ply` (السيستم بيحسبه أوتوماتيك من الطقم).
- لو الفرشة مقاس واحد، الجدول ده بيتملّي بسطر واحد أوتوماتيك.
- إجمالي القطع لكل مقاس = `pieces_in_ply × إجمالي الراق` — ده اللي بيوصل للترقيم.

### 4.8 `LayLine` — سطر التوب
| الحقل | النوع | ملاحظات |
|---|---|---|
| `lay` | FK → Lay | |
| `line_no` | Integer | |
| `roll_ref` | PositiveIntegerField (null) | **مش FK** — جدول الأتواب مش موجود في المرحلة دي، فالـ FK مستحيلة. رقم فاضي بيتحوّل FK بـ migration في المرحلة 3 |
| `article` / `lot_no` / `roll_no` | Char (اختياري) | مفاتيح التعريف من التيكت — بتتسجل نص من دلوقتي |
| `roll_length_m` | Dec(8,2) | طول التوب |
| `plies` | Integer | الراق |
| `remnant_m` | Dec(8,2) | البواقي |
| `shade_note` | Char | **توصيف اللون** — كلام الصنايعي للتفرقة بين الأتواب (أسود / أسود غامق)، نص حر مش قايمة ثابتة |
| `roll_end_action` | Choice | تصرّف نهاية التوب (تحت) |
| `remnant_disposition` | Choice | تلقائي حسب الطول (بند 5.4) |

**`roll_end_action` — التلات حالات الحقيقية:**
| القيمة | المعنى | الأثر على الحساب |
|---|---|---|
| `وصل` | التوب خلص واتوصل بتوب جديد لإكمال نفس الراق | `has_splice = true` على السطر. **إجمالي الراق = Σ plies − عدد الوصلات** — لأن الراق الموصول اتعدّ في السطرين |
| `توب جديد` | خلص لآخره واتبدأ توب جديد في نفس الفرشة | سطر جديد عادي، البواقي = 0 |
| `اتخزن` | الباقي اتخزن لفرشة تانية | يتسجل في البواقي ويرجع للمخزن |

### 4.8.1 `LayShadeBreakdown` — الراق لكل لون

القصة ممكن تكون لون واحد أو اتنين أو تلاتة أو أكتر، والمطلوب معرفة **كام راق لكل لون**، وبالتالي كام قطعة ونسبتها.

| الحقل | النوع |
|---|---|
| `lay` | FK → Lay |
| `shade` | Char — اللون |
| `plies` | Integer — الراق |
| `is_manual` | Bool — اتكتب بالإيد (الوضع السريع) ولا اتحسب |

**طريقتين للدخول، ومكان واحد للقراءة:**

| الوضع | إزاي |
|---|---|
| **تفصيلي** | اللون موجود على السطر أصلاً، فالتجميع **بيتحسب لوحده** مع كل إعادة حساب. مفيش إدخال زيادة خالص |
| **سريع** | مفيش سطور نجمّع منها، فالمشرف يقدر يكتبه. **اختياري بالكامل** — لو سابه فاضي القصة بتتحفظ وبتتقفل عادي |

**القطع لكل لون** = `plies × pieces_per_ply` · **النسبة** = `plies ÷ إجمالي الراق`.

> **الوصل بيتطرح من لون السطر الموصول نفسه.** الراق الموصول اتكتب في سطرين، ولو اتعدّ مرتين مجموع الألوان هيطلع أكبر من إجمالي الراق بواحد لكل وصلة. بالطرح ده **مجموع الألوان بيساوي `total_plies` بالظبط دايمًا**.

**العرض:** شريط ألوان فوق شريط الحسابات في شاشة القصة (لون · راق · قطع · نسبة)، وجدول في صفحة التفاصيل.

**اتساق كتابة اللون:** خانة اللون في السطر بتقترح الألوان اللي اتكتبت في نفس القصة، عشان «أسود» و«اسود» مايبقوش لونين في التجميع.

### 4.9 `LayOutput` — القطع الفعلية (شاشة الترقيم)
| الحقل | النوع |
|---|---|
| `lay` | OneToOne → Lay |
| `actual_pieces` | Integer — إجمالي القطع السليمة (مثال: 490) |
| `rejected_pieces` | Integer — التالف |
| `recorded_by` / `recorded_at` | Audit |
| `notes` | Text — سبب الفرق |

**التوزيع على المقاسات — أوتوماتيك:**
المشرف بيدخّل **الإجمالي بس**، والسيستم بيوزّعه على المقاسات بنسبة الطقم المسجّل للموديل.

```
نصيب المقاس = الإجمالي × (pieces_in_ply للمقاس ÷ pieces_per_ply)
```
**طريقة التوزيع: أكبر باقي (Largest Remainder / Hamilton).**
1. احسب نصيب كل مقاس بالكسر، وخد الجزء الصحيح منه.
2. الفرق بين مجموع الأجزاء الصحيحة والإجمالي = عدد الوحدات المتبقية.
3. وزّع الوحدات دي على المقاسات صاحبة **أكبر كسر متبقي**، واحدة لكل مقاس. عند التعادل، الأصغر مقاسًا الأول.

مثال: طقم `30 32 33 34 36 36 38 38 38` = 9 قطع في الراق (38 نصيبه 3/9، 36 نصيبه 2/9، والباقي 1/9 لكل واحد). لو الإجمالي 490:

| المقاس | النصيب | صحيح | الكسر | +1؟ | النهائي |
|---|---|---|---|---|---|
| 38 | 163.33 | 163 | .33 | — | **163** |
| 36 | 108.89 | 108 | .89 | ✔ | **109** |
| 30 | 54.44 | 54 | .44 | ✔ | **55** |
| 32 | 54.44 | 54 | .44 | ✔ | **55** |
| 33 | 54.44 | 54 | .44 | — | **54** |
| 34 | 54.44 | 54 | .44 | — | **54** |
| **المجموع** | | 487 | | +3 | **490** ✔ |

> المجموع لازم يطابق الإجمالي بالظبط. أي طريقة توزيع مجموعها مش مساوي `actual_pieces` تعتبر باگ.

**التعديل اليدوي:** اختياري ومقفول افتراضيًا. لما المشرف يفتحه:
- بيظهر **تأكيد** ("هتعدّل التوزيع الأوتوماتيكي — متأكد؟")
- بعد الحفظ الحقل بيتعلّم `is_manually_adjusted = true` ويتسجل في الـ Audit log
- تحقق: مجموع المقاسات لازم = `actual_pieces`

**تغيير الطقم:** لو الموديل اتغيّر طقم مقاساته، الفرشات القديمة بتفضل بطقمها القديم (snapshot على مستوى الفرشة، مش reference حي).

---

## 5. قواعد الحساب

### 5.1 وضعا الإدخال
| الوضع | إمتى | المطلوب |
|---|---|---|
| **تفصيلي** | الوضع الافتراضي | سطر لكل توب زي الدفتر (طول / راق / بواقي / لون) |
| **سريع** | لما يكونوا مستعجلين | إجمالي الأمتار + إجمالي الراق بس |

في الوضع السريع بيتعمل **سطر واحد مجمَّع** (`is_aggregate = true`)، والسيستم بيعلّم الفرشة "إدخال سريع" عشان التقارير تفرّق. المشرف يقدر يفكّها لتفصيلي بعدين.

### 5.2 المعادلات الأساسية
```
الميتراج المتوقع (متر/قطعة) = lay_length_m ÷ pieces_per_ply
                              مثال: 9 م ÷ 9 بناطيل = 1 م للقطعة

إجمالي الراق          = Σ (LayLine.plies)      [مع تجاهل تكرار الوصل]
القطع النظرية         = إجمالي الراق × pieces_per_ply
إجمالي أطوال الأتواب  = Σ (LayLine.roll_length_m)
إجمالي البواقي        = Σ (LayLine.remnant_m)
القماش المستهلك       = إجمالي الراق × lay_length_m

الميتراج الحقيقي (متر/قطعة) = إجمالي أطوال الأتواب ÷ actual_pieces
                              مثال: 500 م ÷ 490 قطعة = 1.02 م للقطعة
```

> **مهم:** الميتراج الحقيقي بيستخدم **القطع الفعلية من الترقيم** مش النظرية. لحد ما الترقيم يتسجّل، الفرشة بتعرض "الحقيقي: في انتظار الترقيم".

### 5.3 العجز والانحراف
```
عجز القماش  = إجمالي أطوال الأتواب − (القماش المستهلك + إجمالي البواقي)
فاقد القطع   = القطع النظرية − actual_pieces
انحراف الميتراج % = ((الحقيقي − المتوقع) ÷ المتوقع) × 100
```
- بيتحسب أوتوماتيك عند القفل (عجز القماش) وعند الترقيم (فاقد القطع).
- تجاوز نسبة التسامح (`tolerance_pct`، افتراضي 0.5% للقماش و 2% للقطع) → سبب إلزامي + تنبيه للمشرف + علامة "فيها عجز".

### 5.4 قاعدة البواقي
```
لو remnant_m < 1.00 م  →  يتصنّف هالك (waste)
لو remnant_m ≥ 1.00 م  →  يتصنّف صالح للاستخدام
```
الحد الأدنى (1 متر) قابل للتعديل من الإعدادات.

> في المرحلة الأولى ده **تصنيف وتقرير بس**، مش حركة مخزنية. لما المخزون يشتغل، السجل ده بيتحوّل لأرصدة بواقي فعلية.

### 5.5 قواعد التحقق
| # | القاعدة | الرسالة |
|---|---|---|
| ~~V1~~ | ~~`lay_width_cm` ≤ أقل `width_cm`~~ | **اتشالت.** كانت محتاجة عرض مكتوب لكل توب، والصنايعي اللي بيفرد عارف أضيق توب أصلاً — هو ماسكه. القاعدة مكانتش هتقوله حاجة جديدة مقابل خانة زيادة على كل سطر. اتشالت معاها `LayLine.width_cm` |
| V2 | `plies` > 0 و `roll_length_m` > 0 | |
| V3 | `remnant_m` < `lay_length_m` | "الباقي أكبر من طول الفرشة — كان ينفع راق زيادة" |
| V4 | `roll_length_m` ≈ (`plies` × `lay_length_m`) + `remnant_m` ± التسامح | "أطوال التوب مش مظبوطة" — **تحذير في الوضعين، مش منع أبدًا**. الفرشة بتتعلّم `has_length_mismatch` وبتظهر في التقارير، والقفل بيعدّي. **السبب:** المشرف اللي السيستم رافض يقفّله مش هيرجع يقيس التوب تاني — هيغيّر الرقم. المنع بيجيب صفوف مرتّبة وبيضيّع صفوف صحيحة. |
| V5 | مفيش قفل من غير سطر واحد على الأقل | |
| V6 | مجموع المقاسات = `pieces_per_ply` | "مجموع المقاسات مش مطابق لعدد القطع" (بيتحسب أوتوماتيك) |
| V7 | `team_leader` لازم يكون من الحاضرين في تاريخ الفرشة | تحذير + إمكانية تجاوز بسبب |
| V8 | أكتر من توصيف لون في نفس الفرشة | **مسموح وطبيعي** — التوصيف على مستوى السطر، والسيستم بيعرض تنبيه بسيط: "الفرشة فيها أكتر من درجة لون" عشان القطع اللي هتتجمع مع بعض في البندلة تبقى من نفس الدرجة |
| V11 | مجموع الراق حسب اللون = إجمالي الراق (لو اتكتب بالإيد) | "مجموع الراق حسب اللون (ن) أكتر/أقل من إجمالي الراق (م) بفرق ك" — **تحذير مش منع**، بنفس منطق V4: المنع هيعلّم المشرف يسيبها فاضية، وده بيضيّع الرقم اللي هو عايزه أصلاً |
| V9 | `actual_pieces` > القطع النظرية | "القطع الفعلية أكتر من النظرية — راجع الراق" |

### 5.6 الفرشة الممتدة لأكتر من يوم

الفرشة ممكن تبدأ النهاردة وتخلص بكره، فالتاريخ مش رقم واحد.

- الفرشة ليها **تاريخ بداية** و**تاريخ نهاية**، والنهاية بتتحدد ساعة القفل.
- **الإنتاج بيتحسب على تاريخ الانتهاء** (يوم ما اتقفلت الفرشة) عشان مايتعدش مرتين، والفترة الكاملة بتفضل ظاهرة في التفاصيل.
- **حضور رئيس الفريق** بيتشيّك على **أي يوم داخل الفترة** مش يوم واحد بعينه.
- **الإنتاجية** = القطع الفعلية ÷ مجموع ساعات حضور رئيس الفريق **خلال أيام الفرشة كلها** من البصمة.
- **الفلترة بالتاريخ** بتشتغل بالتقاطع: الفرشة بتظهر في نتيجة أي فترة بتتقاطع مع مدتها، مش بتاريخ البداية بس.
- في القايمة، الفرشة الممتدة بتتعرض `19/5 → 20/5` بدل تاريخ واحد.

---

## 6. الربط بالـ HR وجهاز البصمة

- `Lay.team_leader` = FK على `Employee` من نظام الـ HR الموجود.
- الربط بجهاز البصمة عن طريق **`employee_code`** — مش عن طريق `employee` FK، لأن 17% من سجلات البصمة الـ FK فيها فاضي. الربط على الكود هو اللي بيشتغل فعليًا.
- **`Employee` مالوش علاقة بـ `User`** — كيانين منفصلين. رئيس الفريق `Employee`، والمشرف اللي بيدخّل `User`.
- **مفيش دالة حضور جاهزة.** التطبيق الوحيد لقاعدة "أول بصمة حضور وآخر بصمة انصراف" مدفون جوه `build_weekly_report()` ومش قابل للاستدعاء لموظف واحد. محتاجين ملف جديد `hr/attendance.py` يوفّر: الحاضرين في فترة، وساعات موظف في فترة.
- **سجلات البصمة بتبدأ من 9 يوليو 2026 بس.** أي فرشة أقدم من كده يتخطّى فيها تحقق الحضور بدون تحذير.
- عند اختيار رئيس الفريق، القايمة بترتّب **الحاضرين في أي يوم من أيام الفرشة أولًا** (مش تاريخ الإدخال، لأن الإدخال بيتم متأخر من الدفتر).
- لو المختار مش مسجَّل حضور في أي يوم من الفترة → **تحذير مش منع**، مع خانة سبب اختيارية.

**اللي بنكسبه:**
- إنتاجية رئيس الفريق = إجمالي القطع الفعلية ÷ ساعات الحضور من البصمة خلال أيام الفرشة.
- **الأيام صفر الساعات بتتستبعد.** 25% من أيام-الموظفين فيهم بصمة واحدة بس (دخل وما بصمش خروج)، فساعاتهم بتطلع صفر. القسمة على صفر ممنوعة، والصفر مش حقيقة — الراجل اشتغل بس مابصمش.
- كل تقرير إنتاجية بيعرض **تغطيته** صراحة: "مبني على 12 يوم من أصل 16". لو التغطية أقل من 50% يظهر تحذير إن الرقم مش موثوق.
- الحل الجذري إداري مش برمجي: الالتزام ببصمة الخروج. التقرير بيكشف المشكلة مش بيداريها.
- مقارنة بين رؤساء الفرق على نفس الموديل ونفس البنك.
- أساس جاهز لحساب أجر الإنتاج في مرحلة تانية.

---

## 7. الشاشات (Mobile-first)

> **الأولوية للموبايل.** المشرف بيدخّل من موبايله وهو واقف في الصالة أو بعد الشغل. الكمبيوتر استخدام ثانوي — نفس الشاشات responsive.

### 7.1 قائمة الفرشات (List View)

**الأعمدة الظاهرة:**

| العمود | ملاحظة |
|---|---|
| التاريخ | تاريخ واحد، أو `19/5 → 20/5` لو الفرشة امتدت |
| الكود / الموديل | مع القَصّة (`fit`) تحت الاسم بخط أصغر |
| المقاسات | شيبس مختصرة `30·32·32·34·34·36`، والباقي يظهر بـ "+2" |
| طول الفرشة | متر |
| عدد القطع في الراق | |
| إجمالي القطع | النظرية، والفعلية جنبها لو الترقيم اتسجل |
| الميتراج المتوقع | متر/قطعة |
| الميتراج الحقيقي | متر/قطعة — أو "بانتظار الترقيم" |
| الانحراف | نسبة، ملوّنة (أخضر داخل التسامح / أحمر خارجه) |
| رئيس الفريق · البنك | |
| الحالة | مفتوحة / مقفولة / مترقّمة / معتمدة |
| 📷 | أيقونة صغيرة لو فيه صورة دفتر — بتفتح الصورة مباشرة |

**على الموبايل:** الأعمدة بتتحول لكارت من 3 سطور — (التاريخ · الموديل · القَصّة) / (المقاسات · طول الفرشة) / (المتوقع مقابل الحقيقي + الانحراف).

**كروت الملخص فوق القايمة:** فرشات الفترة · إجمالي القطع · متوسط الميتراج الحقيقي · فرشات فيها عجز · مستنية ترقيم.

**الفرز:** بأي عمود، والافتراضي التاريخ تنازلي.
**التصدير:** إكسيل / PDF بنفس الفلاتر المطبَّقة — `/api/cutting/lays/export/?export=xlsx` وبياخد نفس الـ query params بتاعة القايمة.

### 7.1.1 شريط البحث المتقدم

خانة بحث واحدة بتدوّر في: الكود · اسم الموديل · القَصّة · الخامة · توصيف اللون · رقم اللوط · رقم التوب · اسم رئيس الفريق · الملاحظات.

- بحث فوري (debounce 300ms) وبيقبل عربي وإنجليزي وأرقام.
- **بحث بالصيغ المختصرة (Token search)** للمستخدم السريع:
  - `ميتراج>1.2` — فرشات ميتراجها الحقيقي أعلى من 1.2
  - `عجز:نعم` · `مقاس:32` · `لون:أسود` · `خامة:MEGAN` · `بنك:2` · `حالة:مستنية-ترقيم`
- البحثات المتكررة تتحفظ باسم ("فرشات كارل فيها عجز") وتظهر كأزرار سريعة فوق القايمة.

### 7.1.2 الفلاتر المتقدمة

لوحة جانبية (Drawer على الموبايل) فيها كل علاقات الفرشة:

| المجموعة | الفلاتر |
|---|---|
| الوقت | من تاريخ / إلى تاريخ · اختصارات (النهاردة، الأسبوع، الشهر) |
| الموديل | الموديل · الكود · القَصّة · الفئة (رجالي/حريمي/أطفال) |
| المقاسات | مقاس معيّن موجود في الفرشة · عدد المقاسات · طقم مقاسات محدد |
| القياسات | طول الفرشة (من–إلى) · عرض الفرشة (من–إلى) · إجمالي الراق (من–إلى) |
| القطع | القطع النظرية (من–إلى) · القطع الفعلية (من–إلى) · فاقد القطع % |
| الميتراج | المتوقع (من–إلى) · الحقيقي (من–إلى) · **الانحراف % (من–إلى)** |
| القماش | توصيف اللون · الخامة (`article`) · اللوط · إجمالي الأمتار (من–إلى) |
| البواقي | فيها بواقي؟ · بواقي هالك؟ · إجمالي البواقي (من–إلى) |
| الناس | رئيس الفريق · المشرف اللي دخّل · البنك |
| الحالة | حالة الفرشة · فيها عجز؟ · إدخال سريع؟ · مرحّلة (backfill)؟ · فيها صورة دفتر؟ |

- الفلاتر بتتجمع بـ AND، والقيم داخل نفس الفلتر بـ OR (تختار كذا لون مع بعض).
- الفلاتر المطبَّقة بتظهر كشيبس فوق القايمة، وكل شيب فيه × للإلغاء، وزرار "مسح الكل".
- **حالة الفلاتر بتتخزن في الـ URL** عشان الرابط ينفع يتبعت لحد تاني بنفس النتيجة.

### 7.2 شاشة فرشة جديدة — **الشاشة الأهم**
تصميمها لازم يقلّد صفحة الدفتر بالظبط عشان المشرف ينقل بعينه من غير تفكير:

**قسم علوي (نفس ترتيب الدفتر):**
التاريخ · **الموديل** (بحث من الكتالوج أو إضافة سريعة) · عرض الفرشة · طول الفرشة · البنك · رئيس الفريق

> **البنك ورئيس الفريق مش مكتوبين في الدفتر** (بند 14.5)، فبيظهروا في بلوك منفصل مكتوب عليه كده.

**قيم افتراضية للبنك ورئيس الفريق:**
تقريبًا كل الفرشات بتتفرد على نفس البنك مع نفس رئيس الفريق، فالشاشة **بتختارهم أوتوماتيك** من الإعدادات:

| الحقل | المكان |
|---|---|
| `CuttingSettings.default_bank` | FK → Bank (null) |
| `CuttingSettings.default_team_leader` | FK → Employee (null) |

- الاتنين **قابلين للتغيير في أي فرشة بضغطة واحدة** — مفيش قفل.
- الفرشة اللي بعدها بترجع للقيمة الافتراضية تاني، لحد ما القيمة الافتراضية نفسها تتغيّر من الإعدادات.
- لو الافتراضي فاضي، الخانة بتفضل «اختار…» زي الأول.

**خانة المقاسات:** المقاس بيتكتب لوحده وبيتضاف بزرار «أضف» (أو Enter)، وبيبقى شيب. الدوس على الشيب بيشيل نسخة واحدة منه. السيستم بيحسب **عدد القطع** أوتوماتيك من مجموع الشيبس. لو اختار موديل ليه طقم محفوظ، بيتملّي لوحده وينفع يتعدّل أو يتمسح كله.

> **ليه مش نص متتابع زي ما كان مكتوب هنا؟** الخانة رقمية عشان الكيبورد يطلع أرقام كبيرة (متطلب الإدخال بإيد واحدة تحت)، و**الكيبورد الرقمي على iOS مافيهوش زرار مسافة**. يعني `30 32 32` كانت مستحيلة الكتابة على الموبايل اللي الشاشة اتعملت عشانه. الخانة لسه بتقبل نص كامل لو اتلزق أو اتكتب من كيبورد كامل.

**سويتش في الأعلى:** `تفصيلي` ⟷ `سريع`

**قسم سفلي (تفصيلي) — جدول:**
`طول التوب | الراق | بواقي | اللون | ⋯`
- إضافة سطر بزرار كبير أو Enter من آخر خانة.
- كل سطر فيه قايمة صغيرة لتصرّف نهاية التوب (وصل / توب جديد / اتخزن).
- الباقي بيتلوّن أوتوماتيك: أحمر = هالك (< 1 م)، أخضر = يرجع مخزن.

**قسم سفلي (سريع):** خانتين بس — إجمالي الأمتار + إجمالي الراق.

**شريط حسابات لايف** ثابت تحت الشاشة بيتحدّث مع كل رقم: إجمالي الراق · القطع النظرية · الميتراج المتوقع · العجز.

**زرار كاميرا** لتصوير ورقة الدفتر مباشرة.
**زرار "قفل الفرشة"** بيشغّل كل التحققات.

**متطلبات UX:**
- عربي RTL، خط Tajawal، أرقام إنجليزية.
- خانات إدخال كبيرة، كيبورد رقمي افتراضي، إدخال بإيد واحدة.
- ضغط الصور قبل الرفع.
- Offline-first: الفرشة تتحفظ محليًا (IndexedDB) وتتزامن لما النت يرجع.

### 7.3 الترقيم

**الترقيم بياخد وقت، فمش لازم يبقى خطوة منفصلة دايمًا.** فيه طريقتين والاتنين شغالين:

1. **مع القفل** — خانتين اختياريتين (السليمة / التالف) في شاشة القصة نفسها. لو اتملّوا، القفل والترقيم بيتعملوا
   في **نفس المعاملة**، فمفيش قصة بتفضل مقفولة بترقيم نص. وبيتسجّل مين دخّلها في `LayOutput.recorded_by`
   وفي سجل النشاط.
2. **بعدين** — لو الخانتين فاضيين، القصة بتروح لشاشة الترقيم زي ما كانت.

شاشة الترقيم **اتشالت من القائمة** لأن الطريقة الأولى بقت الطبيعية، وبتتفتح من صفحة القصة نفسها أو من
`/cutting/count` مباشرة.


قايمة بالفرشات المقفولة اللي لسة متسجلش لها قطع فعلية:
```
كود 1747 · موديل X · القطع النظرية 500
[ القطع السليمة: ___ ]   [ التالف: ___ ]   [ ملاحظات ]
                                              [ حفظ ]
```
بمجرد الحفظ، الميتراج الحقيقي والانحراف بيتحسبوا وحالة الفرشة تبقى `مترقّمة`.

### 7.4 صفحة تفاصيل الفرشة (Detail View)

بتتقسم لأقسام واضحة:

**1. الترويسة** — الكود والموديل والقَصّة · التاريخ · البنك · رئيس الفريق · الحالة · أزرار (تعديل / قفل / ترقيم / اعتماد / طباعة).

**2. شريط الأرقام الأساسية** — 6 أرقام كبيرة في صف واحد:
طول الفرشة · عرض الفرشة · إجمالي الراق · إجمالي القطع · الميتراج المتوقع · **الميتراج الحقيقي** (مع الانحراف ملوّن جنبه).

**3. المقاسات** — جدول: المقاس · قطع في الراق · إجمالي القطع · القطع الفعلية (مع علامة لو اتعدّلت يدوي).

**4. سطور الأتواب** — طول التوب · الراق · الباقي (بلونه: هالك/صالح) · توصيف اللون · تصرّف نهاية التوب · الخامة واللوط ورقم التوب · أيقونة صورة التيكت.

**5. الاستهلاك والعجز** — إجمالي أطوال الأتواب · المستهلك · البواقي · العجز، مع شرح مكتوب بجملة واحدة لو فيه عجز.

**6. صورة ورقة الدفتر** — بحجم كبير قابل للتكبير. ده القسم اللي بيتفتح لما حد يشك في رقم.

**7. سجل النشاط** — مين أنشأ، مين قفل، مين رقّم، أي تعديل بعد القفل بسببه.

**زرار طباعة** بيطلع نسخة A4 من الفرشة (مفيدة للأرشيف أو التوقيع).

### 7.5 استلام الأتواب — **مؤجلة**
مش موجودة في المرحلة الأولى لعدم وجود أرصدة افتتاحية. بيانات التوب بتتكتب جوه سطر الفرشة، وصورة التيكت بتترفع.

### 7.6 سجل البواقي — **اتعمل**

`/cutting/remnants` — قايمة **عرض بس**: الطول · التصنيف (هالك/صالح) · اللون · اللوط · الخامة · لينك للقصة.
**من غير رصيد ومن غير زرار "استخدام"** — ده بييجي مع المخزون.

فيه فلتر (الكل / هالك / صالح) وإجماليات، والـ API `GET /api/cutting/remnants/` **للقراءة بس** — أي محاولة كتابة بترجع 405.

> **ملحوظة:** خانتَي `اللوط` و`الخامة` بتتعرضا هنا بس **مفيش مكان في الواجهة بيملاهم** لسه — شوف بند 14.5.

---

## 8. الـ API (DRF)

| Method | Endpoint | الوظيفة |
|---|---|---|
| GET/POST | `/api/cutting/lays/` | قائمة / إنشاء فرشة — بتقبل كل الفلاتر كـ query params (`real_metrage_min`, `deviation_max`, `size`, `color`, `lot_no`, `team_leader`, `has_shortage`, `status`…) عن طريق `django-filter` |
| GET | `/api/cutting/lays/search/` | البحث الحر + الصيغ المختصرة |
| GET/POST | `/api/cutting/saved-filters/` | الفلاتر المحفوظة باسم |
| GET | `/api/cutting/lays/export/` | تصدير إكسيل/PDF بنفس الفلاتر |
| GET/PATCH | `/api/cutting/lays/{id}/` | تفاصيل / تعديل |
| POST | `/api/cutting/lays/{id}/close/` | قفل + تشغيل التحققات. لو فيه تحذيرات، بيرفض لحد ما يتبعت `reason` |
| POST | `/api/cutting/lays/{id}/output/` | تسجيل القطع الفعلية |
| POST | `/api/cutting/lays/{id}/approve/` | اعتماد |
| POST | `/api/cutting/lays/{id}/lines/` | إضافة سطر توب |
| DELETE | `/api/cutting/lay-lines/{id}/` | حذف سطر |
| GET | `/api/cutting/lays/{id}/calculations/` | الحسابات |
| POST | `/api/cutting/lays/{id}/attachments/` | رفع صورة |
| GET/POST/PATCH/DELETE | `/api/cutting/models/` | كتالوج الموديلات — إضافة وتعديل للمشرف، حذف للأدمن |
| GET/POST/PATCH/DELETE | `/api/cutting/fits/` | كتالوج القَصّات — نفس الصلاحيات |
| GET/PATCH | `/api/cutting/settings/1/` | إعدادات القص، ومنها البنك ورئيس الفريق الافتراضيين |
| GET | `/api/cutting/lays/{id}/distribution/?actual_pieces=` | معاينة توزيع الترقيم من غير حفظ |
| GET | `/api/cutting/lays/summary/` | كروت الملخص فوق القايمة |
| GET/POST/DELETE | `/api/cutting/saved-filters/` | البحثات المحفوظة |
| GET | `/api/cutting/lays/{id}/validate/` | بروفة تحققات القفل من غير قفل |
| GET/POST | `/api/cutting/size-sets/` | أطقم المقاسات |
| POST | `/api/cutting/size-sets/parse/` | تفكيك نص المقاسات لسطور وحساب المجموع |
| ~~`/api/cutting/rolls/`~~ | — | **مؤجل لمرحلة المخزون** |
| GET | `/api/cutting/team-leaders/?date=` | رؤساء الفرق (الحاضرين أولًا) |
| GET | `/api/cutting/remnants/` | سجل البواقي (عرض فقط) |
| GET | `/api/cutting/reports/{name}/` | التقارير. `name` = `metrage` · `shortage` · `productivity` · `remnants` · `banks` · `quality` |
| GET | `/api/cutting/notifications/` | تنبيهات المستخدم الحالي بس |
| POST | `/api/cutting/notifications/mark-read/` | تعليم مقروء (كلها أو `ids` محددة) |

> **باراميتر التصدير اسمه `export` مش `format`.** `format` محجوزة لـ DRF عشان يختار بيها الـ renderer، وبترجّع 404 لأي قيمة مالهاش renderer قبل ما الـ view يشتغل أصلًا. فالتصدير بيبقى `?export=xlsx` أو `?export=pdf`، والافتراضي JSON.

---

## 9. التقارير

1. **الميتراج لكل موديل** — المتوقع مقابل الحقيقي والانحراف %.
2. **العجز** — بالتاريخ ورئيس الفريق والخامة واللوط (بيكشف لو لوط معيّن أطواله ناقصة على طول).
3. **إنتاجية رئيس الفريق** — قطع/ساعة مبنية على البصمة.
4. ~~**حركة الأتواب**~~ — **مؤجل للمرحلة 3**: محتاج جدول أتواب وأرصدة افتتاحية، والاتنين مستبعدين في بند 4.3.
5. **البواقي والهالك** — كام متر راح هالك الشهر ده.
6. **تقرير يومي للبنك** — فرشات وقطع لكل بنك.
7. **جودة الإدخال** — نسبة الفرشات المدخَلة بالوضع السريع (مؤشر على انضباط التسجيل).

---

## 10. متطلبات غير وظيفية

- **Mobile-first** — كل الشاشات تشتغل بكفاءة على موبايل، والكمبيوتر ثانوي.
- **عربي RTL** بالكامل، خط Tajawal، أرقام إنجليزية.
- **الأداء:** الحسابات اللايف على الفرونت في أقل من 200ms من غير طلب سيرفر.
- **Offline:** حفظ محلي وتزامن أوتوماتيك.
- **الصور:** ضغط قبل الرفع (حد أقصى 1MB)، تخزين على الـ VPS في `/media/cutting/{year}/{month}/`.
- **Audit log:** كل تعديل بعد القفل يتسجّل (مين، إمتى، القديم والجديد، السبب).
- **الصلاحيات:** RBAC مربوط بنظام الصلاحيات الموجود.

---

## 11. المراحل

| المرحلة | المحتوى | الناتج |
|---|---|---|
| **1 (MVP)** | Model + SizeSet + Lay + LayLine + الحسابات + الوضع السريع + شاشة الترقيم بالتوزيع الأوتوماتيكي + ربط رئيس الفريق بالبصمة + صورة الدفتر | نسخة الدفتر شغالة على الموبايل |
| **2** | تقارير الميتراج والعجز + سجل البواقي والهالك | التحكم في الفاقد من غير مخزون · **اتعمل** |
| **3** | **نظام المخزون المتطور**: جرد افتتاحي بالأمتار + جدول الأتواب + الأرصدة + ربط السطور القديمة بالـ `roll_id` + الباركود | مخزون حقيقي |
| **4** | إنتاجية رؤساء الفرق + الربط بأجر الإنتاج + ترحيل الدفاتر القديمة | الإنتاجية والأرشيف |

---

## 11.1 التنبيهات

| الحدث | القناة | المستقبِل |
|---|---|---|
| فرشة فيها عجز تعدّى نسبة التسامح | **داخل السيستم + إيميل** | مشرف القص + مدير الإنتاج |
| فاقد قطع تعدّى النسبة بعد الترقيم | داخل السيستم + إيميل | نفس المجموعة |
| فرشة مقفولة من أكتر من 24 ساعة ولسة مستنية ترقيم | داخل السيستم | مشرف القص |

**التنفيذ:** جدول `Notification` داخلي (مقروء / غير مقروء) + إيميل **مجمَّع مرة في اليوم**، مش رسالة لكل فرشة، عشان محدّش يتجاهل الإيميلات. مستقبلًا نضيف واتساب لو احتجنا.

> **مفيش Celery ولا django-q.** الـ SRS كان بيسمّيهم، بس الاتنين مش متسطّبين والاتنين مش مستاهلين لشغل بيتنفّذ **مرة في اليوم**: الـ queue بيبقى للشغل اللي لازم يحصل حالًا وممكن يفشل، مش لتقرير مجدول. اللي اتعمل: management command اسمه `send_cutting_digest` على **systemd timer** الساعة ٧ صباحًا — وده اللي السيرفر بيشغّل بيه كل حاجة تانية أصلًا. الملفات في `deploy/`.

**تفاصيل التنفيذ:**

| القرار | السبب |
|---|---|
| صف واحد لكل (فرشة، نوع، مستقبِل) | نفس العجز مايعملش تنبيه جديد كل مرة الفرشة تتحسب |
| `emailed_at` على كل تنبيه | الإيميل مايتبعتش مرتين، والتجميع بيبقى ممكن |
| **«مستنية ترقيم» داخل السيستم بس** | بيتكرر كل يوم لحد ما حد يتصرّف، والإيميل اليومي عنه بيتحوّل لضوضاء الناس بتفلترها |
| المستقبِلين = مشرف القص + مدير الإنتاج + **الأدمن** | الأدمن عنده كل الصلاحيات التانية، وفي تركيب صغير ممكن يكون هو الحساب الوحيد الموجود — من غيره التنبيهات تتكتب لحد مفيش |
| التنبيه مالوش حق يوقع القفل | بيتنادى جوه `notifications.safe()`؛ فشل التنبيه بيتسجّل في اللوج والقفل بيكمّل |
| الإيميل غير مضبوط = طباعة في اللوج | السيرفر من غير SMTP بيفضل شغال بدل ما يقع |
| **التجميع بالعنوان مش بالشخص** | كذا حساب ممكن يشتركوا في إيميل واحد؛ التجميع بالشخص بيبعت لنفس الصندوق نسخة لكل واحد — وده بالظبط التكدّس اللي التقرير المجمّع موجود عشان يمنعه |
| الإرسال عن طريق **Resend** | مفيش SMTP على السيرفر، وResend عبارة عن HTTP API. اتعمل backend لـ Django في `config/email_backends.py` فـ `send_mail` بيفضل شغال زي ما هو |

---

## 12. ترحيل الدفاتر القديمة

مطلوب إدخال دفاتر قديمة مش بس البدء من يوم التشغيل.

- حقل `is_backfill` على `Lay` بيعلّم الفرشة كإدخال أثري.
- **التاريخ الماضي مسموح** بدون تحذير في وضع الترحيل.
- **التحقق من حضور رئيس الفريق يتوقف** في الوضع ده (سجلات البصمة القديمة ممكن تكون ناقصة).
- **مفيش أثر على المخزون** — الفرشات المرحّلة مش بتخصم من رصيد الأتواب، لأن القماش ده اتصرف من زمان.
- **شاشة إدخال سريع للترحيل**: صفحة واحدة مضغوطة، بعد الحفظ بتفضل مفتوحة على نفس البنك والموديل عشان تدخّل الصفحة اللي بعدها على طول.
- التقارير بتفلتر `is_backfill` عشان الإحصائيات التشغيلية متتلخبطش.
- **الأولوية:** الترحيل يتعمل بعد ما السيستم يشتغل على الجديد ويستقر — مش قبله.
- **حجم الترحيل لسه غير محدد.** الخطة: نبني الإدخال اليدوي المتتابع في المرحلة 4، ولو طلع الحجم كبير (أكتر من ~150 صفحة) نضيف استيراد إكسيل بقالب جاهز بدل الإدخال اليدوي.

---

## 13. قرارات معمارية

- **نفس مشروع الـ HR ونفس قاعدة البيانات** — تطبيق Django جديد باسم `cutting` جوه المشروع القايم، وبيستورد `Employee` مباشرة من تطبيق الـ HR من غير API وسيط.
- **الأتواب إدخال يدوي في المرحلة الحالية** — حقل `barcode` موجود في الموديل من دلوقتي بس من غير سكانر، عشان لما تجيبوا سكانر أو تستخدموا كاميرا الموبايل ميحصلش migration مؤلم.
- **مفيش أوامر تشغيل** — الفرشة بتتربط بـ `Model` مباشرة، والموديل بيتكرر فبيتسجل مرة واحدة في الكتالوج.

---

## 14. نقاط متبقية للمرحلة الجاية

1. عيّنات تيكتات إضافية لضبط الإدخال — عندنا دلوقتي تيكتين بس: واحد من KB DENIM (خامة MEGAN) وواحد لخامة BLACK MIMAS من غير اسم مورد ظاهر.
2. نسبة التسامح المناسبة للعجز — نبدأ 0.5% ونعدّل بعد أول شهر بيانات.
3. عدّ الدفاتر القديمة لتحديد أسلوب الترحيل (يدوي ولا استيراد إكسيل).
### 14.5 حقول مالهاش مصدر — مراجعة قبل التشغيل

مراجعة لكل حقل في الموديل: مين بيملاه، ومين بيستخدمه.

**اتشالت خلاص:**

| الحقل | ليه |
|---|---|
| `LayLine.ticket_image` | محدش هيصوّر تيكت لكل توب |
| `LayLine.ticket_data` | كان تابع للصورة |

**بتتعرض في الواجهة بس مفيش مكان بيملاها** — دي أهم مجموعة:

| الحقل | بتتعرض فين | الأثر |
|---|---|---|
| `LayLine.article` (الخامة) | فلاتر القايمة · سجل البواقي · تقرير العجز | **تقرير العجز بيجمّع حسب اللوط والخامة — والاتنين فاضيين دايمًا، فالتجميع بيطلع فاضي** |
| `LayLine.lot_no` (اللوط) | نفس المكان | نفس المشكلة |
| `LayLine.roll_no` | كان في التفاصيل — **اتشال** | — |

> **ده أهم قرار مستني.** إما نضيف خانتين (الخامة واللوط) في سطر التوب، أو نشيل التجميع حسب اللوط من تقرير العجز. دلوقتي التقرير موجود بس مايقدرش يشتغل.

**موجودة في الداتابيز، مفيش لها واجهة، ومحدش بيستخدمها:**

| الحقل | الحالة |
|---|---|
| `LayLine.notes` | بند 14.4 حاجزها لعيوب القماش |
| `Lay.team_members` | بند 4.6 بيقول «للعلم بس» |

**اتشالت** (قرار ١ سبتمبر): `LayLine.width_cm` و `LayLine.net_weight_kg` و `Lay.narrowest_width_cm` ومعاهم **V1**.

**موجودة بقصد ومحجوزة لمرحلة جاية — تُترك:**

| الحقل | السبب |
|---|---|
| `LayLine.roll_ref` | بند 4.3.1 — بتتحوّل FK مع المخزون |
| `LayLine.barcode` | بند 13 — عشان مايحصلش migration مؤلم لما ييجي سكانر |
| `Lay.client_uuid` | idempotency للأوفلاين |
| `Lay.is_backfill` | بند 12 — الترحيل، والتقارير بتستبعدها |

**في الواجهة ومحتاجة تأكيد:**

| الخانة | السؤال |
|---|---|
| `تصرّف نهاية التوب` (وصل/توب جديد/اتخزن) | تلات أزرار على **كل سطر**، ومش ظاهرة في صفحة الدفتر. **بتغيّر إجمالي الراق** (الوصل بيطرح راق)، فلو المشرف مش هيضغطها الحساب هيبقى غلط في القصات اللي فيها وصل |

> **الاتنين دول (الخامة/اللوط · أزرار الوصل) موقوفين لحد تجربة صفحات دفتر حقيقية** — متتبنيش ولا تتشال قبل كده.

---

4. **عيوب القماش مؤجلة عن قصد** — مش في نطاق المرحلة دي. حقل `notes` على `LayLine` كافي مؤقتًا لو حد حب يكتب ملاحظة، ولما تيجي مرحلة الجودة نضيف تسجيل عيب بصورة ومكان العيب على التوب.
