# MR.Mekawy Factory ERP — دليل المشروع

نظام إدارة مصنع بنطلونات جينز. بيغطي دلوقتي حاجتين: **شؤون العاملين والحضور** (مربوط بجهاز بصمة ZKTeco)، و**القص والفرد** (تسجيل الفرشات على البنوك وحساب الميتراج والعجز).

الوثيقة دي بتشرح **إيه الموجود وليه اتعمل كده**. المواصفات الوظيفية للقص في ملف منفصل: `SRS-cutting-bank-rolls.md`.

---

## ١ · نظرة سريعة

| | |
|---|---|
| **الرابط** | `https://factory.mekawyerp.shop` |
| **الباك** | Django 5.2 + DRF 3.17 + SimpleJWT · gunicorn على `127.0.0.1:8010` |
| **الفرونت** | Next.js 16 (App Router) + React 19 + TypeScript + Tailwind v4 · على `3002` |
| **الداتابيز** | PostgreSQL على **بورت 5433** · `factory_erp` (إنتاج) و `factory_erp_dev` (تطوير) |
| **الواجهة** | عربي RTL بالكامل · خط Cairo عمومًا و Tajawal في شاشات القص |
| **الخوادم** | `factory-erp-backend.service` · `factory-erp-frontend.service` · `factory-erp-cutting-digest.timer` |
| **nginx** | بيوجّه `/api` و `/media` و `/admin` و `/iclock` لـ Django، وأي حاجة تانية لـ Next |

### حجم الكود

| المكان | ملفات | سطور |
|---|---|---|
| `backend/cutting` | 34 | 6,875 |
| `backend/hr` | 16 | 1,180 |
| `backend/devices` | 11 | 880 |
| `frontend/app` | 21 | 5,154 |
| `frontend/lib` + `components` | 9 | 1,504 |
| `frontend/tests/ui` | 5 | 1,009 |

**293 تست باك** + **١١٤ تحقّق في متصفح حقيقي**.

---

## ٢ · بنية المستودع

```
factory_erp/
├── backend/
│   ├── config/            إعدادات Django · urls · backend الإيميل
│   ├── hr/                الموظفون · الحضور المحسوب · التقرير الأسبوعي
│   ├── devices/           بروتوكول جهاز البصمة ZKTeco
│   ├── cutting/           موديول القص (الأكبر)
│   ├── media/             الصور المرفوعة
│   ├── .env               الأسرار — خارج git
│   └── dev.sh             يشغّل manage.py على قاعدة التطوير
├── frontend/
│   ├── app/               صفحات Next.js
│   ├── lib/               api client · حسابات لايف · تعريف الفلاتر
│   ├── components/Shell   القائمة الجانبية والصلاحيات
│   └── tests/ui/          اختبارات المتصفح (Playwright)
├── deploy/                وحدات systemd + تعليمات النشر
├── reference/             صورة صفحة الدفتر الحقيقية
├── SRS-cutting-bank-rolls.md   مواصفات القص
└── PROJECT.md             الملف ده
```

---

## ٣ · الأدوار والصلاحيات

الأدوار **مجموعات Django عادية** (`auth_group`)، مش موديل مخصص. دور المستخدم = أول مجموعة يلاقيها في القايمة دي، والـ superuser دايمًا `admin`:

```python
ROLE_ORDER = ["admin", "hr", "production_manager", "cutting_supervisor", "cutting"]
```

| الدور | بيشوف | بيعمل |
|---|---|---|
| `admin` | كل حاجة | كل حاجة، وهو الوحيد اللي **بيمسح** من الكتالوج |
| `hr` | الموظفين والحضور والتقرير الأسبوعي | إدارة الموظفين والبصمات — **مالوش دخل بالقص** |
| `production_manager` | كل موديول القص | **قراءة بس** |
| `cutting_supervisor` | كل موديول القص | يدخّل الفرشات ويقفل ويرقّم، ويضيف/يعدّل في الكتالوج |
| `cutting` | كل موديول القص | **نفس `cutting_supervisor` بالظبط** — حساب المشرف الحقيقي هيبقى فيها |

الصلاحيات متعرّفة مرتين بقصد: `backend/cutting/permissions.py` هي الحقيقة، و`frontend/lib/roles.ts` بتحدد اللي يظهر في القائمة بس. **الفرونت مش حاجز أمني** — أي طلب بيتحقق في الباك.

---

## ٤ · موديول `hr` — الموظفون والحضور

### الموديلات

| الموديل | الغرض |
|---|---|
| `Department` | الأقسام |
| `Employee` | الموظف. **`employee_code` هو رقمه على جهاز البصمة** ومفتاح الربط الوحيد |
| `WorkSchedule` | singleton: بداية ونهاية الدوام وأيام الإجازة |
| `SiteSettings` | singleton: اسم النظام وأيقوناته |

> **`Employee` مالوش أي علاقة بـ `User`.** كيانين منفصلين تمامًا: رئيس الفريق `Employee`، والمشرف اللي بيدخّل `User`.

### الحضور — مش مخزّن، بيتحسب

مفيش جدول «حضور وانصراف». فيه **بصمات خام** في `devices.AttendanceLog`، والساعات بتتحسب وقت العرض بقاعدة واحدة:

> لكل موظف في كل يوم: **أول بصمة = حضور، آخر بصمة = انصراف**، والساعات = الفرق بينهم.

القاعدة دي مطبّقة في مكانين بقصد:

- `hr/reports.py` → `build_weekly_report()` — التقرير الأسبوعي، بيبني كل الموظفين مرة واحدة
- `hr/attendance.py` → `hours_for()` — **قابلة للاستدعاء لموظف واحد على فترة**، وموديول القص بينادي عليها

التكرار متعمد وموثّق بتعليق في الملفين: التقرير الأسبوعي شغال وصحيح ومستخدم يوميًا، وإعادة كتابته فوق الدالة الجديدة مكسب صفر ومخاطرة حقيقية. **لو القاعدة نفسها اتغيّرت، لازم يتغيّروا مع بعض.**

### تلات حقائق في البيانات بتحكم التصميم

1. **١٧٪ من البصمات `employee_id` فيها `NULL`.** الربط بيتم على `employee_code` دايمًا، مش على الـ FK.
2. **٢٥٪ من أيام-الموظفين فيها بصمة واحدة بس** — دخل وما بصمش خروج. اليوم ده ساعاته صفر، **وبيتستبعد** من أي متوسط أو معدل، مش بيتحسب كصفر. الراجل اشتغل، هو بس مابصمش.
3. **سجلات البصمة بتبدأ من ٩ يوليو ٢٠٢٦.** أي حاجة أقدم من كده غير قابلة للتحقق، مش غائبة — والتحقق بيتخطاها بصمت.

### الـ endpoints

```
GET  /api/me/                      الدور والاسم
GET  /api/dashboard/               أرقام لوحة التحكم
CRUD /api/employees/               + /fingerprints/ /enroll-fp/ /push-to-device/ /attendance/
CRUD /api/departments/  /api/users/
GET  /api/schedule/  /api/settings/  /api/settings/public/
GET  /api/reports/weekly/          JSON · /export/ إكسيل · /pdf/ (A4 بصور، A5 مضغوط)
```

---

## ٥ · موديول `devices` — جهاز البصمة

بيتكلم **ADMS / PUSH** بتاع ZKTeco. **الجهاز هو اللي بيبدأ كل اتصال** — وده اللي بيخلّيه شغال من ورا NAT المصنع من غير أي إعداد شبكة.

```
GET  /iclock/cdata?SN=..&options=all    مصافحة، السيرفر بيرد بالإعدادات
POST /iclock/cdata?SN=..&table=ATTLOG   البصمات
POST /iclock/cdata?SN=..&table=OPERLOG  المستخدمين والبصمات والعمليات
GET  /iclock/getrequest?SN=..           الجهاز بيسأل: فيه أوامر ليا؟
POST /iclock/devicecmd?SN=..            نتيجة تنفيذ الأمر
```

الردود **نص عادي**، ومفيش JWT — الجهاز مش بيقدر يصادق، فالتعريف بالرقم التسلسلي.

| الموديل | الغرض |
|---|---|
| `Device` | الجهاز · آخر اتصال · `wipe_requested_at` |
| `AttendanceLog` | بصمة واحدة. `UniqueConstraint(device, employee_code, timestamp)` بيمنع التكرار |
| `FingerprintTemplate` | قالب البصمة base64 لكل (موظف، إصبع) |
| `DeviceCommand` | طابور أوامر للجهاز: تحديث مستخدم · تسجيل بصمة · إعادة تشغيل · مسح |

`devices/services.py` فيها بنّائين الأوامر. الأمر اللي الجهاز مايأكدهوش بيتبعت تاني بعد مدة — ممكن يكون فاته الرد أو كان بيرفع بيانات متأخرة.

**حماية المسح:** بعد طلب مسح، أي بيانات جاية من الجهاز بتتجاهل لمدة ١٥ دقيقة، عشان المسح يبدأ نضيف فعلاً ومايفضلش الجهاز يعيد ملء الجداول. المهلة موجودة عشان الحارس نفسه مايعلّقش للأبد لو الجهاز ماأكدش.

---

## ٦ · موديول `cutting` — القص والفرد

### إيه اللي بيعمله

الصنايعية بيكتبوا في **دفتر ورق** على البنك. المشرف بيدخّل الصفحة من موبايله بعدين. السيستم بيحسب الميتراج والعجز، وبيربط الفرشة برئيس الفريق المتعرَّف عليه من البصمة.

المرحلة دي **من غير أي مخزون**. مفيش رصيد أتواب ولا خصم — الأرقام كلها مقفولة جوه الفرشة نفسها. ده قرار مش تبسيط: مفيش جرد افتتاحي، وأي رصيد هيبدأ غلط وهيفضل غلط.

### بنية الملفات

| الملف | مسؤوليته |
|---|---|
| `models.py` | ١٤ موديل. القواعد اللي على مستوى السطر في `clean()` |
| `services.py` | **كل الحسابات.** المصدر الوحيد للحقيقة |
| `sizes.py` | تفكيك نص المقاسات + توزيع القطع بأكبر باقي |
| `validators.py` | V1 → V10 |
| `search.py` | البحث المختصر (`ميتراج>1.2`) → نفس فلاتر django-filter |
| `filters.py` | كل مجموعات الفلاتر في بند 7.1.2 |
| `reports.py` | ٦ تقارير + إكسيل + PDF |
| `notifications.py` | قواعد التنبيهات |
| `serializers.py` · `views.py` | طبقة رفيعة — **مفيهاش حساب ولا قواعد** |
| `permissions.py` | بوابات الأدوار |
| `management/commands/send_cutting_digest.py` | التقرير اليومي |

### الموديلات

**الكتالوجات** (بيتعمروا مرة وبيتكرر استخدامهم)

| الموديل | ملاحظات |
|---|---|
| `Bank` | ترابيزة الفرد |
| `Category` | القسم (رجالي · حريمي · مواليد · رجالي جامبو …). **كتالوج المصنع بيزوّد فيه**، و**كل موديل لازم يحمل واحد** |
| `GarmentModel` | الموديل. **بيتعرّف بالاسم** («كارل رجالي»)، و`code` بيتولّد لوحده. الكلاس اسمه `GarmentModel` مش `Model` عشان الأخير بيحجب `django.db.models.Model` |
| `SizeSet` | طقم المقاسات. `sizes_raw` نص، و`total_pieces` بيتحسب منه في `save()` |
| `CuttingSettings` | singleton: نسب التسامح · حد الباقي الهالك · **البنك ورئيس الفريق الافتراضيين** |

**الفرشة وتوابعها**

| الموديل | ملاحظات |
|---|---|
| `Lay` | القصة — الكيان الأساسي. `code` **فريد** (الرقم اللي فوق صفحة الدفتر). حقول مدخلة + **١١ حقل محسوب ومخزّن** |
| `LaySizeBreakdown` | توزيع المقاسات — **snapshot** مش مرجع حي |
| `LayShadeBreakdown` | الراق لكل لون — محسوب من السطور (تفصيلي) أو مكتوب (سريع) |
| `LayLine` | سطر توب واحد = سطر في الدفتر. **مفيش صورة تيكت** — محدش هيصوّر تيكت لكل توب |
| `LayOutput` | نتيجة الترقيم (واحد لواحد مع الفرشة) |
| `RemnantLog` | سجل بواقي معلوماتي — **من غير رصيد** |
| `LayAudit` | مين غيّر إيه بعد القفل وليه |
| `Notification` | تنبيه داخلي + طابور الإيميل |
| `SavedFilter` | بحث محفوظ باسم |

### الكود بتاع مين؟

الرقم اللي فوق صفحة الدفتر (`1749`) هو **كود القصة، مش كود الموديل**. الموديل الواحد بيتقص عشرات المرات، وكل مرة بتاخد كود جديد عشان القصتين مايتلغبطوش.

| | |
|---|---|
| `Lay.code` | **بيتكتب بالإيد**، فريد، وهو اللي في الدفتر |
| `GarmentModel.code` | **بيتولّد** — `1, 2, 3…` — مجرد مقبض داخلي، محدش بيكتبه |

الموديل بيتعرّف **بالاسم**: تكتب «كارل رجالي» تلاقيه، ولو مش موجود بيتضاف بالاسم ده نفسه مع قسمه.

> ده كان غلط في أول تصميم — الكود كان على الموديل. اتصحّح بعد أول استخدام حقيقي للشاشة.

### ليه الأرقام مخزّنة مش `property`؟

ده أهم قرار معماري في الموديول، وهو عكس الحدس.

**السبب الحاسم:** بند 7.1.2 بيطلب فلترة **بمدى** على الانحراف % والميتراج الحقيقي، و**`django-filter` مايقدرش يفلتر على Python property** — لازم عمود حقيقي في الداتابيز.

وفيه سببين تانيين:
- **N+1**: قايمة ٥٠ فرشة × حساب بيمشي على السطور = ٥٠ استعلام إضافي على الأقل
- **الأرقام لقطة زمنية**: الفرشة المعتمدة أرقامها اتثبتت. لو غيّرنا معادلة بكرة، الفرشات القديمة **مايتغيروش بأثر رجعي**. الـ property هتغيّرهم كلهم

```
الفرونت يحسب ليعرض (لايف، أقل من 200ms)
الباك يحسب ليحفظ عند القفل والترقيم
        ↓
   الباك هو الحكم
```

المعادلات مكتوبة مرتين بقصد — مرة TypeScript للعرض ومرة Python للحفظ — والتستات بتقفل على أرقام الباك. `frontend/lib/cutting.ts` مكتوب في أول سطر فيه إنه نسخة للعرض وإن الباك هو المرجع.

### المعادلات

```
الميتراج المتوقع  = lay_length_m ÷ pieces_per_ply
إجمالي الراق      = Σ plies − عدد الوصلات
القطع النظرية     = إجمالي الراق × pieces_per_ply
القماش المستهلك   = إجمالي الراق × lay_length_m
عجز القماش        = Σ أطوال الأتواب − (المستهلك + البواقي)
الميتراج الحقيقي  = Σ أطوال الأتواب ÷ actual_pieces      ← القطع الفعلية، مش النظرية
انحراف الميتراج % = ((الحقيقي − المتوقع) ÷ المتوقع) × 100
```

**قاعدة الوصل:** التوب اللي خلص في نص راق والتوب اللي بعده كمّله — الراق ده اتكتب في **السطرين**. عدّه مرتين بيضخّم كل رقم بعديه، فبيتطرح واحد لكل وصلة.

**الميتراج الحقيقي بيقسم على القطع اللي اتعدّت فعلاً.** القسمة على النظرية كانت هتخبّي كل قطعة ضاعت. فيه تست بيتأكد إن الرقمين مختلفين.

### توزيع القطع على المقاسات — أكبر باقي (Hamilton)

المشرف بيدخّل الإجمالي بس، والسيستم بيوزّعه:

1. نصيب كل مقاس بالكسر، وناخد الجزء الصحيح
2. الفرق بين مجموع الأجزاء الصحيحة والإجمالي = وحدات متبقية
3. الوحدات دي تروح لأكبر الكسور، واحدة لكل مقاس. **عند التعادل، الأصغر مقاسًا الأول**

الحساب بـ `Fraction` مش `float` — عشان التعادل يبقى تعادل حقيقي مش صدفة تقريب.

> **المجموع لازم يساوي الإجمالي بالظبط.** فيه تست بيعدّي على كل رقم من ٠ لـ ٩٩٩ ويتأكد.

> **ملحوظة تاريخية:** مثال الـ SRS الأصلي (إصدار 1.8) كان مجموعه ٤٨٨ مش ٤٩٠، وكان بيناقض القاعدة المكتوبة جنبه. اتصلّح في 1.9.

### إدخال المقاسات

**المقاسات بتتضاف واحد ورا التاني، مش بتتكتب كنص واحد.** خانة صغيرة + زرار «أضف»، وكل مقاس بيبقى شيب، والدوس على الشيب بيشيل **نسخة واحدة** منه (فـ `32 ×2` بيبقى `32`).

> **ليه؟** الخانة `inputMode="numeric"` عشان الكيبورد يطلع أرقام كبيرة — وده كان مطلوب صراحة للإدخال بإيد واحدة. لكن **الكيبورد الرقمي على iOS مافيهوش زرار مسافة أصلاً**، يعني `30 32 32` كانت **مستحيلة الكتابة** على الموبايل اللي الشاشة اتعملت عشانه. الاختيار كان بين كيبورد صغير أو نظام إدخال مايحتاجش مسافة — والتاني أحسن على الموبايل بأي حال.

الخانة لسه بتقبل نص كامل لو اتلزق أو اتكتب من كيبورد فيه مسافة، والـ **Enter** بيضيف زي الزرار.

التفكيك بيقبل: مسافات · فواصل عربية وإنجليزية · شرط · **أقواس `(32)(34)` زي ما الدفتر بيكتبها** · **أرقام عربية `٣٠ ٣٢`**.

**التكرار مقصود** — المقاس ممكن يتكرر في نفس الراق، و`30 32 32 34 34 36` معناها `pieces_per_ply = 6` مش 4.

> الفاصلة العربية `،` واقعة جوه نطاق الحروف العربية في Unicode — لو النطاق اتحافظ عليه كامل، `"30، 32"` بيتقرا مقاس اسمه `"30،"`. باگ حقيقي اتمسك في التست.

### قواعد التحقق V2 → V11

| # | القاعدة | المستوى | فين |
|---|---|---|---|
| V2 | الراق وطول التوب > صفر | 🔴 منع | `clean()` + `CheckConstraint` |
| V3 | الباقي < طول الفرشة | 🔴 منع | `clean()` |
| V4 | طول التوب ≈ (راق × طول) + باقي | ⚠️ **تحذير في الوضعين** | `validators` |
| V5 | مفيش قفل من غير سطر | 🔴 منع | القفل |
| V6 | مجموع المقاسات = القطع في الراق | 🔴 منع | القفل |
| V7 | رئيس الفريق حاضر في أي يوم من الفرشة | ⚠️ تحذير | القفل |
| V8 | أكتر من درجة لون | ℹ️ معلومة | القفل |
| V9 | القطع الفعلية ≤ النظرية | 🔴 منع | الترقيم |
| V10 | صورة الدفتر مطلوبة | 🔴 منع | القفل |
| V11 | مجموع الراق حسب اللون = إجمالي الراق | ⚠️ تحذير | القفل |

**التحقق كله في الموديل والخدمة، مش في الـ serializer.** يعني الأدمن والشل والـ API كلهم بيخبطوا في نفس الحيطة.

**ليه V4 تحذير مش منع؟** المشرف اللي السيستم رافض يقفّله **مش هيرجع يقيس التوب تاني** — هيغيّر الرقم لحد ما الشاشة تقبله. المنع بيجيب صفوف مرتّبة وبيضيّع صفوف صحيحة. الفرشة بتتعلّم `has_length_mismatch` وبتظهر في التقارير، والقفل بيعدّي.

**الأخطاء بترجع بالعربي وبرقم القاعدة:**

```json
400 {
  "detail": "الفرشة مش جاهزة للعملية دي",
  "issues": [{ "code": "V6", "level": "error",
               "message": "مجموع المقاسات (5) مش مطابق لعدد القطع في الراق (6)",
               "field": "size_breakdown", "line_no": null }]
}
```
الواجهة بتطابق على `code` مش على نص الرسالة. و`level` بيقول تعمل إيه: `error` بيمنع، `warning` بيطلب سبب، `info` بيتعرض وخلاص.

### دورة حياة الفرشة

```
مفتوحة ──[قفل]──► مقفولة ──[ترقيم]──► مترقّمة ──[اعتماد]──► معتمدة
```

- **القفل** بيشغّل V1–V8 و V10، بيثبّت الأرقام، بيسجّل البواقي، وبيرفع تنبيه لو فيه عجز
- **التعديل بعد الحفظ** من زرار «تعديل» في التفاصيل: الكود والتواريخ والبنك ورئيس الفريق والملاحظات. القصة المفتوحة بتتعدّل مباشرة، والمقفولة بتطلب **سبب إلزامي** بيتسجّل في `LayAudit`
- **الترقيم** بيسجّل القطع، بيوزّعها على المقاسات، بيحسب الميتراج الحقيقي، وبيرفع تنبيه لو الفاقد كبير. **ينفع يتعمل مع القفل في نفس الطلب** لو القطع معروفة، أو بعدين من شاشة الترقيم — والاتنين في نفس المعاملة فمفيش قصة بتفضل مقفولة بترقيم نص
- **التعديل بعد القفل** بيحتاج **سبب إلزامي** بيتسجّل في `LayAudit`

### الفرشة الممتدة والفلترة بالتقاطع

الفرشة ممكن تبدأ النهاردة وتخلص بكره.

- **`end_date` مش بيبقى فاضي أبدًا.** الـ SRS كان بيسمح بـ `NULL` بمعنى «نفس اليوم»، بس ساعتها كل استعلام تقاطع محتاج `COALESCE` — وده **بيلغي استخدام الفهرس**. بيتملّي بـ `start_date` عند الحفظ
- الفلترة **بالتقاطع**: `WHERE start_date <= :to AND end_date >= :from` — استعلام بسيط بيستخدم الفهرس
- **الإنتاج بيتحسب على يوم القفل** عشان مايتعدش مرتين
- **حضور رئيس الفريق** بيتشيّك على أي يوم داخل الفترة

فيه تست بيدوّر على اليوم اللي في **نص** فرشة اليومين وبيلاقيها، وجنبه assertion بيثبت إن فلتر `start_date` العادي كان هيفوّتها.

### الإنتاجية والتغطية

```
الإنتاجية = القطع الفعلية ÷ ساعات رئيس الفريق خلال أيام الفرشة
```

**الأيام صفر الساعات بتتستبعد من المقام** — مش بتتحسب صفر. وكل رقم إنتاجية بيعرض **تغطيته صراحة**: «مبني على ١٢ يوم من أصل ١٦»، ولو التغطية أقل من ٥٠٪ بيظهر تحذير إن الرقم مش موثوق.

لو مفيش ولا يوم صالح، بيرجّع **لا شيء مع السبب** — مش رقم متلفّق. الحل الجذري إداري مش برمجي: الالتزام ببصمة الخروج. التقرير بيكشف المشكلة مش بيداريها.

---

## ٧ · الـ API

```
# الفرشات
GET/POST   /api/cutting/lays/                      + كل فلاتر 7.1.2
GET        /api/cutting/lays/search/?q=            البحث المختصر
GET        /api/cutting/lays/summary/              كروت الملخص
GET        /api/cutting/lays/export/?export=xlsx   تصدير بنفس الفلاتر
GET/PATCH  /api/cutting/lays/{id}/
POST       /api/cutting/lays/{id}/close/           قفل (+ reason لو فيه تحذير)
GET        /api/cutting/lays/{id}/validate/        بروفة القفل من غير قفل
GET        /api/cutting/lays/{id}/distribution/    معاينة توزيع الترقيم
POST       /api/cutting/lays/{id}/output/          تسجيل القطع
POST       /api/cutting/lays/{id}/approve/
GET        /api/cutting/lays/{id}/calculations/    الحسابات + الإنتاجية
POST       /api/cutting/lays/{id}/lines/  /attachments/
DELETE     /api/cutting/lay-lines/{id}/

# الكتالوجات
CRUD       /api/cutting/models/   /fits/   /banks/   /size-sets/
POST       /api/cutting/size-sets/parse/            تفكيك المقاسات لايف
GET/PATCH  /api/cutting/settings/1/

# المساعدة
GET        /api/cutting/team-leaders/?date_from=&date_to=
GET        /api/cutting/remnants/                   عرض بس
CRUD       /api/cutting/saved-filters/
GET        /api/cutting/notifications/  /unread_count/
POST       /api/cutting/notifications/mark-read/
GET        /api/cutting/reports/{name}/?export=xlsx|pdf
```

> **باراميتر التصدير اسمه `export` مش `format`.** DRF حاجز `format` عشان يختار بيه الـ renderer، وبيرجّع 404 لأي قيمة مالهاش renderer **قبل ما الـ view يشتغل أصلاً**. كلّف تلات تستات فاشلة لحد ما اتكشف.

### البحث المختصر

```
ميتراج>1.2   عجز:نعم   مقاس:32   لون:أسود
خامة:MEGAN   بنك:2     حالة:مستنية-ترقيم   انحراف<-2
```

**التوكن مابيتحولش لكود فلترة خاص** — بيتحوّل **لنفس الـ query parameters اللي `LayFilter` بياخدها**. يعني الاختصار مجرد طريقة أسرع للكتابة، والفلترة مكتوبة مرة واحدة. وبيقبل أرقام عربية، وبيخلط التوكنز مع الكلام العادي، و**التوكن اللي مايعرفوش بيسيبه في نص البحث بدل ما يرميه** — يمكن يكون رقم لوط فيه نقطتين.

---

## ٨ · التقارير والتنبيهات

### التقارير الستة

| التقرير | بيكشف إيه |
|---|---|
| الميتراج لكل موديل | المتوقع مقابل الحقيقي والانحراف |
| **العجز** | بالتاريخ ورئيس الفريق والخامة، **ومجمّع حسب اللوط** — لوط بيتكرر معناه إن أتوابه ناقصة على طول |
| إنتاجية رؤساء الفرق | قطعة/ساعة من البصمة، مع التغطية |
| البواقي والهالك | كام متر راح هالك |
| البنوك اليومي | فرشات وقطع لكل بنك |
| جودة الإدخال | نسبة الإدخال السريع وبدون صورة ومستنية ترقيم |

كلهم بيستبعدوا **الفرشات المرحّلة** افتراضيًا — صفحة دفتر من قبل ما السيستم يشتغل بتشوّه بالظبط الأرقام اللي التقرير موجود عشان يراقبها.

النمط زي `hr/reports.py`: **dict واحد بيغذّي JSON وإكسيل وPDF** من خلال كاتب واحد مقاد بقايمة `columns` بتاعة التقرير نفسه.

> تقرير «حركة الأتواب» (رقم ٤ في الـ SRS) **مش مبني** — محتاج جدول أتواب وأرصدة افتتاحية، والاتنين مؤجلين لمرحلة المخزون.

### التنبيهات

| الحدث | القناة |
|---|---|
| عجز تعدّى التسامح عند القفل | داخل السيستم + إيميل |
| فاقد قطع تعدّى التسامح عند الترقيم | داخل السيستم + إيميل |
| فرشة مقفولة +٢٤ ساعة من غير ترقيم | **داخل السيستم بس** |

التالت مالوش لحظة يتعلّق فيها — **الحدث هو إن مفيش حاجة حصلت** — فبيتلقط بمسح يومي. وهو **مش بيتبعت في الإيميل** لأنه بيتكرر كل يوم لحد ما حد يتصرّف، وإيميل يومي عنه هو اللي بيعلّم الناس تفلتر المُرسِل.

**التنبيه مالوش حق يوقّع القفل.** بيتنادى جوه `notifications.safe()` — لو فشل بيتسجّل في اللوج والقفل بيكمّل. فيه تست بيخلّي التنبيه يرمي exception وبيتأكد إن الفرشة اتقفلت.

### التقرير اليومي بالإيميل

`send_cutting_digest` على **systemd timer الساعة ٧:٠٠** — قبل الوردية.

> **مفيش Celery ولا django-q.** الـ SRS كان بيسمّيهم، بس الـ queue بيبقى للشغل اللي لازم يحصل حالًا وممكن يفشل، مش لتقرير مجدول بيتنفّذ مرة في اليوم. management command على timer هو اللي السيرفر بيشغّل بيه كل حاجة تانية.

**التجميع بالعنوان مش بالشخص.** كذا حساب ممكن يشتركوا في إيميل واحد؛ التجميع بالشخص بيبعت لنفس الصندوق نسخة لكل واحد. والسطور داخل الإيميل مجمّعة بـ **(الفرشة، النوع)** مش بصف التنبيه، لأن العجز الواحد بيكتب صف لكل مستقبِل.

**الإرسال عن طريق Resend** — مفيش SMTP على السيرفر، وResend عبارة عن HTTP API. اتعمل backend لـ Django في `config/email_backends.py` فـ `send_mail` بيفضل شغال زي ما هو. لو الإعداد مش موجود، بيطبع في اللوج بدل ما يقع.

---

## ٩ · الفرونت

| الصفحة | بتعمل إيه |
|---|---|
| `/` | لوحة التحكم |
| `/employees` · `/attendance` · `/reports/weekly` | شؤون العاملين |
| `/devices` · `/users` · `/settings` | إدارة النظام |
| **`/cutting/new`** | **الشاشة الأهم** — إدخال الفرشة |
| `/cutting` | القايمة + البحث + الفلاتر |
| `/cutting/[id]` | التفاصيل (أقسام بند 7.4 السبعة) |
| `/cutting/count` | الترقيم — **مش في القائمة**، بيتفتح من القصة نفسها. العدّ الطبيعي بيتكتب مع القفل |
| `/cutting/models` · `/cutting/categories` · `/cutting/size-sets` | الكتالوجات |
| `/cutting/remnants` | سجل البواقي — **عرض بس**، مفيش رصيد ولا زرار استخدام |
| `/cutting/reports` · `/cutting/notifications` | التقارير والتنبيهات |

### شاشة الفرشة الجديدة

**ترتيبها من الدفتر بالحرف** (شوف `reference/notebook-page-1749.jpeg`):

```
التاريخ (من / إلى) · كود القصة · الموديل · المقاسات · عرض الفرشة · طول الفرشة · عدد القطع
جدول:  طول التوب | الراق | الباقي | اللون
```

**البنك ورئيس الفريق مش في الدفتر** — في بلوك منفصل مكتوب عليه كده، بدل ما يتدسّوا في الترويسة كأن الصنايعي نسي يكتبهم. والاتنين **بيتختاروا أوتوماتيك** من الإعدادات وقابلين للتغيير بضغطة.

**العرض بالمتر زي الدفتر.** الدفتر بيكتب `١,٦٢ م` والـ API بيخزّن سنتيمتر. الخانة بتقرا اللي هو كاتبه — مفيش حاجة في المصنع عرضها ٢٠ سم ولا ٢٠ متر — وتحتها مكتوب دايمًا **«هيتسجّل ١٦٢ سم»**.

**تفكيك المقاسات بيمرّ على السيرفر** (`size-sets/parse/`) مش على parser تاني في TypeScript، فالشاشة والداتابيز مايختلفوش على معنى `30 32 32`. الفرونت بيقسّم محليًا بس عشان يعرف يضيف ويشيل شيب — والعدّ النهائي من السيرفر.

**الصورة بتتضغط قبل الرفع** على canvas من غير أي مكتبة — كاميرا الموبايل بتطلع ٤–٨ ميجا، والحد ١ ميجا.

### حالة الفلاتر في الـ URL

**الـ URL هو نفسه استعلام الـ API.** مفيش ترجمة بينهم. عشان كده «ابعت الرابط لحد تاني يشوف نفس النتيجة» حقيقة — وفيه تست بيفتح نفس الرابط في متصفح تاني ويقارن الصفوف.

### RTL — تلات فخاخ

1. **الرقم السالب** بيتعرض `19.65-` بدل `-19.65`
2. **متتالية أرقام بمسافات** بتتقلب: تكتب `30 32 32 34 34 36` وتشوفها بالعكس
3. **كود لاتيني جنب عربي** بيتلغبطوا

الحل: كل خانة رقمية `dir="ltr"` عن طريق `.ltr-num`، والأكواد اللاتينية جوه `<bdi>`. **التلاتة اتكشفوا في متصفح حقيقي، مش في مراجعة كود** — وفيه تحققات عليهم عشان مايرجعوش.

---

## ١٠ · الاختبارات

### الباك — ٢٩٣ تست

```bash
cd backend && venv/bin/python -m pytest
```

| الملف | بيغطي |
|---|---|
| `test_sizes.py` | التفكيك والتوزيع — بما فيه **كل إجمالي من ٠ لـ ٩٩٩** |
| `test_calculations.py` | المعادلات · الوصل · العجز · الميتراج الحقيقي |
| `test_validators.py` | V1 → V10 |
| `test_lay_dates.py` | الفرشة الممتدة · التقاطع · ساعات البصمة |
| `test_api.py` | الصلاحيات لكل دور × كل فعل · الفلاتر · الانتقالات |
| `test_search.py` | البحث المختصر |
| `test_reports.py` | التقارير الستة + التصدير |
| `test_notifications.py` | التنبيهات · التقرير اليومي · backend الإيميل |

وفي المتصفح: `sizes-entry.mjs` بيتأكد إن الإدخال شغال **من غير أي زرار مسافة**.

التستات بتشتغل على **PostgreSQL** زي الإنتاج، على داتابيز اختبار مبنية من `factory_erp_dev`.

### المتصفح — ١١٤ تحقّق

`frontend/tests/ui/*.mjs` — Playwright على مقاس موبايل، بيشغّل الشاشات الحقيقية على بيئة staging منفصلة.

> **Playwright مش dependency في المشروع** — المتصفحات كانت على السيرفر. تشغيل الاختبارات محتاج بيئة staging شغالة، والتعليمات في ترويسة كل ملف.

### البيئة المنفصلة

```bash
# باك على قاعدة التطوير
cd backend && DB_NAME=factory_erp_dev ALLOWED_HOSTS=127.0.0.1,localhost DEBUG=True \
  venv/bin/python manage.py runserver 127.0.0.1:8011
# فرونت مربوط عليه
cd frontend && BACKEND_URL=http://127.0.0.1:8011 npx next dev -p 3010 --hostname 127.0.0.1
```

**كل اختبار في المتصفح بيشتغل هنا. الإنتاج مابيتلمسش.**

---

## ١١ · التشغيل

### قاعدتين بيانات

```bash
./dev.sh migrate                    # التطوير
venv/bin/python manage.py migrate   # الإنتاج — بموافقة صريحة بس
```

**قاعدة ثابتة: أي migration على الإنتاج بيتسأل عليه الأول، حتى لو مجرد إضافة عمود.**

### قبل أي migration على الإنتاج

```bash
pg_dump -h 127.0.0.1 -p 5433 -U factory_erp -Fc factory_erp > /root/db-backups/prod-$(date +%F-%H%M).dump
# والأهم: اتأكد إنها بترجع فعلاً، مش إنها موجودة بس
createdb factory_erp_verify && pg_restore -d factory_erp_verify prod-....dump
```

### النشر

```bash
cd frontend && npm run build
systemctl restart factory-erp-backend factory-erp-frontend
```

> **الـ build لوحده مش نشر.** Next بيقرا المسارات وقت التشغيل — لو بنيت وماعملتش restart، الصفحة الجديدة بترجّع **404** والسيرفر مبسوط ومفيش خطأ في اللوج.

### التقرير اليومي

```bash
systemctl list-timers factory-erp-cutting-digest.timer
venv/bin/python manage.py send_cutting_digest --dry-run
```

---

## ١٢ · حاجات مقصودة، ومحدودة

### مقصودة

| القرار | السبب |
|---|---|
| مفيش مخزون أتواب | مفيش جرد افتتاحي؛ أي رصيد هيبدأ غلط ويفضل غلط |
| `roll_ref` رقم عادي مش FK | جدول الأتواب مش موجود؛ بيتحوّل FK بـ migration في مرحلة المخزون |
| المعادلات مكتوبة مرتين | الفرونت للعرض اللايف، الباك للحفظ — والباك هو الحكم |
| قاعدة الحضور مكتوبة مرتين | التقرير الأسبوعي شغال ومستخدم؛ إعادة كتابته مكسب صفر ومخاطرة حقيقية |
| `end_date` مش فاضي أبدًا | `COALESCE` بيلغي الفهرس على أهم استعلام في النظام |
| المشرف يضيف ويعدّل بس مايمسحش | هو اللي بيضيف، يبقى هو اللي بيغلط؛ ومنعه من التصحيح بيسيب الاسم الغلط في كل التقارير |

### محدودة

- **Resend شغال بمُرسِل تجريبي** (`onboarding@resend.dev`) — بيوصّل لصاحب الحساب بس لحد ما دومين يتفعّل
- **الأوفلاين مش مبني** — بس مفيش حاجة بتمنعه: الفرشة بتتبعت في request واحد، وفيه `client_uuid` للـ idempotency
- **تقرير حركة الأتواب** مؤجل لمرحلة المخزون
- **ترحيل الدفاتر القديمة** — الحقل `is_backfill` موجود والتقارير بتستبعده، بس مفيش شاشة ترحيل مخصصة لسه

---

## ١٣ · المصطلحات

| بالعربي | بالإنجليزي | يعني |
|---|---|---|
| البنك | Spreading Bank | ترابيزة الفرد |
| التوب | Fabric Roll | رول القماش الواحد |
| الفرشة | Lay / Spread | طبقات القماش المفرودة فوق بعض |
| الراق | Plies | عدد الطبقات |
| عدد القطع | Pieces per Ply | القطع اللي بتطلع من الراق الواحد |
| البواقي | Remnants | الفاضل من التوب اللي مكفاش راق كامل |
| الميتراج | Consumption / piece | استهلاك القماش للقطعة |
| الخامة | Article | اسم القماش على التيكت |
| توصيف اللون | Shade note | كلام الصنايعي للتفرقة بين درجات الأتواب |
| القصة | Cutting run / Lay | عملية القص الواحدة — ليها كود لوحدها |
| القسم | Category | رجالي · حريمي · مواليد · رجالي جامبو … |
| وصل | Splice | التوب خلص واتوصل بتوب جديد لإكمال نفس الراق |
